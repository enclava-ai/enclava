---
phase: 06-toasts-confirmations
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - frontend/src/app/layout.tsx
  - frontend/src/app/budgets/page.tsx
  - frontend/src/app/admin/api-keys/page.tsx
  - frontend/src/app/admin/connectors/page.tsx
  - frontend/src/app/admin/pricing/page.tsx
  - frontend/src/app/admin/users/page.tsx
  - frontend/src/components/admin/UserManagement.tsx
  - frontend/src/components/plugins/PluginManager.tsx
  - frontend/scripts/check-client-plumbing.sh
autonomous: true
requirements: [FDBK-03, GUARD-02]
user_setup: []
must_haves:
  truths:
    - "Native `confirm()`, `alert()`, and `prompt()` calls are gone from frontend TSX source."
    - "Destructive flows use `useConfirm()` or `ConfirmDialog` with clear consequence copy."
    - "`npm run check:plumbing` enforces native-dialog detection by default."
---

<objective>Replace native confirmation flows with themed confirmations and enforce the dialog guardrail.</objective>
<context>
@.planning/phases/06-toasts-confirmations/06-CONTEXT.md
@.planning/phases/06-toasts-confirmations/06-RESEARCH.md
@.planning/phases/06-toasts-confirmations/06-PATTERNS.md
@.planning/phases/06-toasts-confirmations/06-VALIDATION.md
@frontend/src/components/ui/confirm-dialog.tsx
</context>
<tasks>
<task type="auto">
  <name>Task 1: Mount confirm provider and migrate native confirms</name>
  <files>frontend/src/app/layout.tsx, frontend/src/app/budgets/page.tsx, frontend/src/app/admin/api-keys/page.tsx, frontend/src/app/admin/connectors/page.tsx, frontend/src/app/admin/pricing/page.tsx, frontend/src/app/admin/users/page.tsx, frontend/src/components/admin/UserManagement.tsx, frontend/src/components/plugins/PluginManager.tsx</files>
  <action>Mount `ConfirmProvider`, replace all native confirm flows with `useConfirm()`, and preserve existing destructive action behavior after confirmation.</action>
  <acceptance_criteria>
    - Native dialog scan from `06-VALIDATION.md` returns no matches.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Enforce native-dialog guardrail</name>
  <files>frontend/scripts/check-client-plumbing.sh</files>
  <action>Make the Phase 5 plumbing guardrail fail on native dialogs by default after source is clean.</action>
  <acceptance_criteria>
    - `cd frontend && npm run check:plumbing` exits 0 on the cleaned tree.
    - A temporary `confirm("delete?")` sample is detected.
  </acceptance_criteria>
</task>
</tasks>
<verification>
- [ ] native dialog scan from `06-VALIDATION.md` returns no matches
- [ ] `cd frontend && npm run check:plumbing`
- [ ] `cd frontend && npm run lint`
- [ ] `cd frontend && npm run build`
</verification>
<success_criteria>
- All native dialogs are replaced with themed confirmation flows.
- Confirm provider covers migrated call sites.
- Plumbing guardrail catches native dialog regressions by default.
</success_criteria>
