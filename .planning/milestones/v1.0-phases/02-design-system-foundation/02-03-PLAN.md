---
phase: 02-design-system-foundation
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - frontend/src/app/globals.css
  - frontend/tailwind.config.js
  - frontend/src/components/ui/badge.tsx
  - frontend/src/components/ui/status-badge.tsx
  - frontend/src/components/ui/confirm-dialog.tsx
  - frontend/src/components/ui/page-header.tsx
  - frontend/src/components/ui/empty-state.tsx
  - frontend/src/components/ui/skeletons.tsx
autonomous: true
requirements: [DS-01, DS-02, DS-03, DS-04]
user_setup: []
must_haves:
  truths:
    - "Foundation lint and build checks pass after token and primitive additions."
    - "No app-wide color sweep, navigation shell rewrite, LLM route movement, toast migration, or preview route work is included in Phase 2."
    - "Standalone TypeScript check is run and any unrelated existing blocker is documented."
---

<objective>
Verify the design-system foundation and document any unrelated blockers.
</objective>

<context>
@.planning/phases/02-design-system-foundation/02-VALIDATION.md
@frontend/package.json
@frontend/tsconfig.json
@frontend/eslint.config.mjs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Run source and scope verification</name>
  <files>frontend/src/app/globals.css, frontend/tailwind.config.js, frontend/src/components/ui</files>
  <read_first>.planning/phases/02-design-system-foundation/02-VALIDATION.md</read_first>
  <action>Run source greps proving tokens, mappings, and primitives exist. Verify no committed preview route was added and no deferred route/navigation moves occurred.</action>
  <acceptance_criteria>
    - `rg -n "--success-soft|--warning-soft|--danger-soft|--info-soft" frontend/src/app/globals.css` returns matches.
    - `rg -n "<alpha-value>|accent-soft|border-strong|success|warning|danger|info" frontend/tailwind.config.js` returns matches.
    - `find frontend/src/app/_dev frontend/src/app/debug frontend/src/app/rag-demo frontend/src/app/test-auth -maxdepth 0 2>/dev/null` returns no output.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Run frontend quality gates</name>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/eslint.config.mjs</files>
  <read_first>frontend/package.json, frontend/tsconfig.json, frontend/eslint.config.mjs</read_first>
  <action>Run `cd frontend && npm run lint`, `cd frontend && npx tsc --noEmit`, and `cd frontend && npm run build`. Fix Phase 2-caused failures. If the known TypeScript 6 config deprecation issue remains, document it as unrelated.</action>
  <acceptance_criteria>
    - `cd frontend && npm run lint` exits 0.
    - `cd frontend && npm run build` exits 0.
    - `cd frontend && npx tsc --noEmit` exits 0 or summary documents the unrelated TypeScript 6 deprecation output.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `cd frontend && npm run lint`
- [ ] `cd frontend && npx tsc --noEmit`
- [ ] `cd frontend && npm run build`
</verification>

<success_criteria>
- Phase 2 requirements are satisfied or any unrelated validation blocker is explicitly documented.
- Phase 3 can consume the token and primitive foundation.
</success_criteria>
