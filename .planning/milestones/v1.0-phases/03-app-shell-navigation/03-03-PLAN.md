---
phase: 03-app-shell-navigation
plan: "03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - frontend/src/components/ui/navigation.tsx
  - frontend/src/app/layout.tsx
  - frontend/src/app/llm/page.tsx
  - frontend/src/app/settings/llm/page.tsx
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04]
user_setup: []
must_haves:
  truths:
    - "Shell navigation parity, LLM redirects, lint, and build are verified."
    - "No route cleanup regression or deferred color/toast/plumbing work is included."
---

<objective>Verify shell and LLM IA integration.</objective>
<context>
@.planning/phases/03-app-shell-navigation/03-VALIDATION.md
@frontend/package.json
@frontend/tsconfig.json
@frontend/eslint.config.mjs
</context>
<tasks>
<task type="auto">
  <name>Task 1: Run source route and nav checks</name>
  <files>frontend/src</files>
  <read_first>.planning/phases/03-app-shell-navigation/03-VALIDATION.md</read_first>
  <action>Run source greps proving nav parity hooks, `/settings/llm` links, and direct redirects. Verify deleted routes remain absent.</action>
  <acceptance_criteria>
    - `rg -n "/settings/llm|Open navigation|lg:pl-64" frontend/src/components/ui/navigation.tsx frontend/src/app` returns matches.
    - `find frontend/src/app/debug frontend/src/app/rag-demo frontend/src/app/test-auth -maxdepth 0 2>/dev/null` returns no output.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Run frontend gates</name>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/eslint.config.mjs</files>
  <read_first>frontend/package.json, frontend/tsconfig.json, frontend/eslint.config.mjs</read_first>
  <action>Run lint, standalone TypeScript, and build. Fix Phase 3-caused issues and document unrelated known TypeScript config deprecation if present.</action>
  <acceptance_criteria>
    - `cd frontend && npm run lint` exits 0.
    - `cd frontend && npm run build` exits 0.
    - `cd frontend && npx tsc --noEmit` exits 0 or summary documents the unrelated TypeScript 6 deprecation output.
  </acceptance_criteria>
</task>
</tasks>
<verification>
- [ ] `cd frontend && npm run lint`
- [ ] `cd frontend && npx tsc --noEmit`
- [ ] `cd frontend && npm run build`
</verification>
