---
phase: 01-delete-deprecated-routes
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/src/components/ui/navigation.tsx
autonomous: true
requirements: [CLN-02, CLN-03]
user_setup: []
must_haves:
  truths:
    - "D-04: No frontend navigation, link, import, or source reference points at chatbot, zammad, /debug, test-auth, or rag-demo."
    - "D-05: Backend connector functionality and non-route zammad-style connector code are preserved."
    - "Frontend lint, type check, and build commands pass after deprecated route deletion."
  artifacts:
    - path: "frontend/src/components/ui/navigation.tsx"
      provides: "Active navigation without deprecated route links"
      contains: "Dashboard"
    - path: "frontend/package.json"
      provides: "Frontend quality commands"
      contains: "\"lint\""
  key_links:
    - from: "frontend/src/components/ui/navigation.tsx"
      to: "deleted route paths"
      via: "absence of hrefs"
      pattern: "chatbot|zammad|/debug|test-auth|rag-demo"
---

<objective>
Reconcile references after route deletion and verify the frontend remains import-clean.

Purpose: Ensure Phase 1 satisfies route hygiene requirements rather than only deleting files.
Output: Any stale route references removed from active files, plus lint/type/build verification.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-delete-deprecated-routes/01-CONTEXT.md
@.planning/phases/01-delete-deprecated-routes/01-RESEARCH.md
@.planning/phases/01-delete-deprecated-routes/01-PATTERNS.md
@frontend/src/components/ui/navigation.tsx
@frontend/package.json
@frontend/tsconfig.json
@frontend/eslint.config.mjs
</context>

<threat_model>
- **T2-01 Hidden stale route reference:** A link or import outside the deleted files could keep a dead path visible or break compilation. Mitigation: run the root `rg` scan after Plan 01-01 and edit every remaining non-deleted match.
- **T2-02 Over-broad grep cleanup:** Blindly deleting every `debug` string could remove legitimate backend/internal diagnostic code. Mitigation: only remove matches to `chatbot`, `zammad`, `/debug`, `test-auth`, `rag-demo`, and `rag/debug` when they refer to deprecated frontend routes or deleted frontend proxies.
- **T2-03 False confidence from lint only:** Deleted App Router files can leave type/build issues that lint does not catch. Mitigation: run `npm run lint`, `npx tsc --noEmit`, and `npm run build`.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Remove remaining deprecated route references from active files</name>
  <files>frontend/src/components/ui/navigation.tsx</files>
  <read_first>frontend/src/components/ui/navigation.tsx, .planning/phases/01-delete-deprecated-routes/01-RESEARCH.md, .planning/phases/01-delete-deprecated-routes/01-PATTERNS.md</read_first>
  <action>D-04: After Plan 01-01 has deleted route files, run `rg -n "chatbot|zammad|/debug|test-auth|rag-demo" frontend/src`. For each remaining match in an active file, remove the nav item, link, import, or call that points at the deleted route. If the scan returns no matches, leave `frontend/src/components/ui/navigation.tsx` unchanged and record that no reference cleanup was needed in the summary. Do not restructure navigation, move LLM, add sidebar components, or perform color cleanup in this phase.</action>
  <verify>rg -n "chatbot|zammad|/debug|test-auth|rag-demo" frontend/src returns no output</verify>
  <acceptance_criteria>
    - `rg -n "chatbot|zammad|/debug|test-auth|rag-demo" frontend/src` returns no output.
    - `rg -n "href=.*(/debug|test-auth|rag-demo)|href=.*chatbot|href=.*zammad" frontend/src` returns no output.
    - `git diff -- frontend/src/components/ui/navigation.tsx` contains no sidebar, drawer, LLM route movement, or color-token migration changes.
  </acceptance_criteria>
  <done>No active frontend source file references the deprecated route names or paths.</done>
</task>

<task type="auto">
  <name>Task 2: Run frontend import and build verification</name>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/eslint.config.mjs</files>
  <read_first>frontend/package.json, frontend/tsconfig.json, frontend/eslint.config.mjs, .planning/phases/01-delete-deprecated-routes/01-VALIDATION.md</read_first>
  <action>D-05: Run the frontend quality gate commands from the repo root: `cd frontend && npm run lint`, `cd frontend && npx tsc --noEmit`, and `cd frontend && npm run build`. If a command fails because of a dangling import or stale reference caused by Phase 1 deletions, fix that stale reference in the owning active frontend file and rerun the failed command. Do not delete backend connector functionality, `frontend/src/components/connectors`, or non-route zammad-style connector code to make checks pass. Do not fix unrelated pre-existing lint/type/build failures unless they block proof of this phase; if unrelated failures remain, capture the exact command and error in the summary.</action>
  <verify>cd frontend && npm run lint && npx tsc --noEmit && npm run build</verify>
  <acceptance_criteria>
    - `cd frontend && npm run lint` exits 0 or the summary records an unrelated pre-existing failure with exact output.
    - `cd frontend && npx tsc --noEmit` exits 0 or the summary records an unrelated pre-existing failure with exact output.
    - `cd frontend && npm run build` exits 0 or the summary records an unrelated pre-existing failure with exact output.
    - `git status --short frontend/src/app frontend/src/components frontend/src/hooks frontend/src/types frontend/src/lib` shows no unexpected additions of deprecated route files.
  </acceptance_criteria>
  <done>Frontend route deletion is verified against lint, type, and build checks, or any unrelated blocker is explicitly documented.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- Verification result proving no active references to deleted deprecated routes remain.
- Potential edit: `frontend/src/components/ui/navigation.tsx` only if a stale deprecated route link exists at execution time.
- D-04 reference cleanup coverage.
- D-05 backend/non-route connector preservation coverage.
- No new code symbols are created by this plan.

<verification>
Before declaring plan complete:
- [ ] `rg -n "chatbot|zammad|/debug|test-auth|rag-demo" frontend/src` returns no output.
- [ ] `cd frontend && npm run lint` exits 0 or unrelated failure is documented.
- [ ] `cd frontend && npx tsc --noEmit` exits 0 or unrelated failure is documented.
- [ ] `cd frontend && npm run build` exits 0 or unrelated failure is documented.
- [ ] No deleted-route page or proxy files are reintroduced.
</verification>

<success_criteria>
- All tasks completed.
- No active frontend references point at deprecated routes.
- Frontend import graph is validated by lint/type/build or any unrelated blocker is recorded with exact output.
- Navigation shell, LLM IA, color sweep, and design-system work remain deferred to later phases.
</success_criteria>

<output>
After completion, create `.planning/phases/01-delete-deprecated-routes/01-02-SUMMARY.md`.
</output>
