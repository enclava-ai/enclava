---
phase: 05-spa-api-plumbing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/login/page.tsx
  - frontend/src/app/admin/connectors/page.tsx
  - frontend/src/components/plugins/PluginNavigation.tsx
autonomous: true
requirements: [PLUMB-01]
user_setup: []
must_haves:
  truths:
    - "Internal product navigation does not use full-page reload or new-tab browser APIs."
    - "External OAuth provider redirect remains intentionally external."
    - "Plugin active route detection uses App Router state instead of `window.location.pathname`."
---

<objective>Replace internal full-page and browser-global navigation patterns with Next App Router primitives.</objective>
<context>
@.planning/phases/05-spa-api-plumbing/05-CONTEXT.md
@.planning/phases/05-spa-api-plumbing/05-RESEARCH.md
@.planning/phases/05-spa-api-plumbing/05-PATTERNS.md
@.planning/phases/05-spa-api-plumbing/05-VALIDATION.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Replace internal login and callback navigation</name>
  <files>frontend/src/app/login/page.tsx, frontend/src/app/admin/connectors/page.tsx</files>
  <action>Use `router.replace()` for the successful login dashboard transition and for clearing connector OAuth callback query params. Preserve the external OAuth provider redirect in `AddConnectorDialog` as a documented exception.</action>
  <acceptance_criteria>
    - Login success routes to `/dashboard` without assigning `window.location.href`.
    - Connector callback cleanup removes `oauth_success` and `connector_id` with App Router navigation.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Replace plugin active path window read</name>
  <files>frontend/src/components/plugins/PluginNavigation.tsx</files>
  <action>Use `usePathname()` once in `PluginNavigation` and pass that pathname to child nav items instead of reading `window.location.pathname` during render.</action>
  <acceptance_criteria>
    - Plugin active state remains based on the current pathname.
    - `PluginNavigation` no longer reads `window.location.pathname`.
  </acceptance_criteria>
</task>
</tasks>
<verification>
- [ ] navigation scan from `05-VALIDATION.md` returns no matches
- [ ] `cd frontend && npm run lint`
- [ ] `cd frontend && npm run build`
</verification>
<success_criteria>
- Internal login routing uses App Router APIs and preserves successful authentication flow.
- Connector OAuth callback cleanup uses App Router query replacement without a page reload.
- Plugin navigation active state uses `usePathname()`.
- External OAuth authorization remains a documented external browser redirect.
</success_criteria>
