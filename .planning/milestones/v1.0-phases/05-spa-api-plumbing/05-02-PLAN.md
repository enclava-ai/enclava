---
phase: 05-spa-api-plumbing
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/lib/api-client.ts
  - frontend/src/components/settings/ConfidentialityDashboard.tsx
  - frontend/src/components/ui/user-menu.tsx
  - frontend/src/components/admin/UserManagement.tsx
  - frontend/scripts/check-client-plumbing.sh
  - frontend/package.json
autonomous: true
requirements: [PLUMB-02, PLUMB-03, GUARD-02]
user_setup: []
must_haves:
  truths:
    - "Real client-component JSON backend calls use `apiClient`."
    - "Route-handler fetches, API-client internals, token refresh, file downloads, proxy helpers, and documentation samples are explicit guardrail exceptions."
    - "A plumbing guardrail catches future internal navigation and raw client fetch regressions."
---

<objective>Replace real client fetch calls with `apiClient` and add plumbing guardrails.</objective>
<context>
@.planning/phases/05-spa-api-plumbing/05-CONTEXT.md
@.planning/phases/05-spa-api-plumbing/05-RESEARCH.md
@.planning/phases/05-spa-api-plumbing/05-PATTERNS.md
@.planning/phases/05-spa-api-plumbing/05-VALIDATION.md
@frontend/src/lib/api-client.ts
</context>
<tasks>
<task type="auto">
  <name>Task 1: Convert real client fetches</name>
  <files>frontend/src/lib/api-client.ts, frontend/src/components/settings/ConfidentialityDashboard.tsx, frontend/src/components/ui/user-menu.tsx, frontend/src/components/admin/UserManagement.tsx</files>
  <action>Add `apiClient.patch` and replace real client-component JSON fetch calls with `apiClient` methods. Preserve behavior, request payloads, and user-facing error handling.</action>
  <acceptance_criteria>
    - `ConfidentialityDashboard`, `UserMenu`, and `UserManagement` contain no raw `fetch(` calls.
    - Existing connector PATCH call sites are backed by `apiClient.patch`.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Add client plumbing guardrail</name>
  <files>frontend/scripts/check-client-plumbing.sh, frontend/package.json</files>
  <action>Add an executable guardrail and npm script that fail on internal browser navigation and raw client fetch regressions while excluding documented server/helper/sample paths. Include a native-dialog detector path for Phase 6 without failing the current Phase 5 tree on dialogs Phase 6 owns.</action>
  <acceptance_criteria>
    - `cd frontend && npm run check:plumbing` exits 0 on the cleaned tree.
    - A temporary internal `window.location.href = "/dashboard"` sample is detected.
    - A temporary client `fetch("/api/example")` sample is detected.
  </acceptance_criteria>
</task>
</tasks>
<verification>
- [ ] `cd frontend && npm run check:plumbing`
- [ ] navigation scan from `05-VALIDATION.md` returns no matches
- [ ] client fetch scan from `05-VALIDATION.md` returns no unapproved client-component matches
- [ ] `cd frontend && npm run lint`
- [ ] `cd frontend && npm run build`
</verification>
<success_criteria>
- Real client-component JSON API calls in the planned files use `apiClient`.
- `apiClient.patch` exists and supports existing PATCH call sites.
- `npm run check:plumbing` passes on the cleaned tree.
- Temporary internal navigation and raw client fetch samples are detected by the guardrail.
</success_criteria>
