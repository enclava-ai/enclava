---
phase: 07-loading-empty-a11y
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/src/app/rag/page.tsx
  - frontend/src/app/admin/api-keys/page.tsx
  - frontend/src/app/admin/connectors/page.tsx
  - frontend/src/app/budgets/page.tsx
  - frontend/src/components/rag/collection-manager.tsx
  - frontend/src/components/rag/document-browser.tsx
  - frontend/src/components/agent/AgentConfigManager.tsx
autonomous: true
requirements: [STATE-02]
must_haves:
  truths:
    - "High-traffic zero states use the shared EmptyState primitive."
    - "Each converted empty state has explanatory value copy and a working primary action."
---

<objective>Convert high-traffic one-off empty states into designed onboarding moments.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Convert admin resource empty states</name>
  <files>frontend/src/app/admin/api-keys/page.tsx, frontend/src/app/admin/connectors/page.tsx, frontend/src/app/budgets/page.tsx</files>
  <read_first>
    - frontend/src/components/ui/empty-state.tsx
    - frontend/src/app/admin/api-keys/page.tsx
    - frontend/src/app/admin/connectors/page.tsx
    - frontend/src/app/budgets/page.tsx
  </read_first>
  <action>Replace bespoke Card-based empty states for API keys, connectors, and budgets with `EmptyState`. Preserve existing primary actions: API keys and budgets open their create dialogs with `setShowCreateDialog(true)`, connectors opens the add connector dialog with `setIsAddDialogOpen(true)`. Use active copy: "Create your first API key", "Connect your first data source", and "Start tracking spend".</action>
  <acceptance_criteria>
    - Each target file imports `EmptyState` from `@/components/ui/empty-state`.
    - API keys empty action still calls `setShowCreateDialog(true)`.
    - Budgets empty action still calls `setShowCreateDialog(true)`.
    - Connectors empty action still calls `setIsAddDialogOpen(true)`.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Convert RAG and agent empty states</name>
  <files>frontend/src/app/rag/page.tsx, frontend/src/components/rag/collection-manager.tsx, frontend/src/components/rag/document-browser.tsx, frontend/src/components/agent/AgentConfigManager.tsx</files>
  <read_first>
    - frontend/src/components/ui/empty-state.tsx
    - frontend/src/app/rag/page.tsx
    - frontend/src/components/rag/collection-manager.tsx
    - frontend/src/components/rag/document-browser.tsx
    - frontend/src/components/agent/AgentConfigManager.tsx
  </read_first>
  <action>Replace bespoke empty states for RAG collections, unfiltered RAG documents, and agents with `EmptyState`. Pass an upload-tab handler from `frontend/src/app/rag/page.tsx` to `DocumentBrowser` so the unfiltered no-documents state can switch to the existing Upload Documents tab. Keep filtered document results distinct: when search/filter criteria are active, the empty copy must tell the user to adjust search or filters instead of offering upload/onboarding copy.</action>
  <acceptance_criteria>
    - The empty-state adoption scan in `07-VALIDATION.md` reports `EmptyState` in all six target files.
    - RAG collections empty action still opens the create collection dialog.
    - Agents empty action still opens the create agent dialog.
    - Filtered document empty copy mentions adjusting search or filters.
    - `cd frontend && npm run lint` exits 0.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] empty-state adoption scan from `07-VALIDATION.md`
- [ ] `cd frontend && npm run lint`
</verification>

<success_criteria>
- High-traffic empty states use consistent layout and spacing.
- Empty-state copy explains value and offers the natural next action.
- Filtered zero-results states remain search/filter-specific.
</success_criteria>

## Artifacts This Phase Produces

- No new exported symbols.
