---
phase: 04-color-sweep
plan: "05"
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - frontend/tailwind.config.js
  - frontend/src/app/globals.css
  - frontend/scripts/check-no-hardcoded-colors.sh
  - frontend/package.json
autonomous: true
requirements: [COL-01, COL-02, COL-05, GUARD-01]
user_setup: []
must_haves:
  truths:
    - "Root frontend/src color grep reports zero disallowed UI hits or documented non-UI exceptions."
    - "Legacy `empire`/`enclava` Tailwind and CSS definitions are removed after usage is gone."
    - "A guardrail script catches future hardcoded color regressions."
---

<objective>Remove legacy palette definitions and add hardcoded-color guardrails.</objective>
<context>
@.planning/phases/04-color-sweep/04-VALIDATION.md
@frontend/tailwind.config.js
@frontend/src/app/globals.css
@frontend/package.json
</context>
<tasks>
<task type="auto">
  <name>Task 1: Delete legacy palette definitions</name>
  <files>frontend/tailwind.config.js, frontend/src/app/globals.css</files>
  <action>Remove `empire`/`enclava` Tailwind color blocks and legacy `.empire-*`, `.enclava-*`, `.text-glow`, and `.enclava-glow` CSS classes after root usage is clean. Preserve semantic tokens, chart vars, and font vars.</action>
  <acceptance_criteria>
    - `rg -n "empire|enclava|text-glow|enclava-glow" frontend/tailwind.config.js frontend/src/app/globals.css` returns no legacy palette definitions.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Add guardrail script</name>
  <files>frontend/scripts/check-no-hardcoded-colors.sh, frontend/package.json</files>
  <action>Add an executable script and npm script that fail on disallowed hardcoded UI colors and legacy palette class names while avoiding known non-UI false positives such as server proxy host strings.</action>
  <acceptance_criteria>
    - Guardrail script exits 0 on cleaned tree.
    - Guardrail script intentionally detects a temporary `bg-red-500` or `empire-gold` sample when tested outside committed source.
  </acceptance_criteria>
</task>
</tasks>
<verification>
- [ ] root color scan returns zero disallowed UI hits
- [ ] `cd frontend && npm run lint`
- [ ] `cd frontend && npm run build`
- [ ] `cd frontend && npm run check:colors`
</verification>
