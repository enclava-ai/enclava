---
phase: 04-color-sweep
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/app/rag/**
  - frontend/src/components/rag/**
  - frontend/src/app/extract/**
  - frontend/src/components/extract/**
autonomous: true
requirements: [COL-01, COL-02, COL-03]
user_setup: []
must_haves:
  truths:
    - "RAG and Extract surfaces have no legacy palette UI classes."
    - "Document/collection/template statuses use semantic status treatment."
---

<objective>Sweep RAG, Extract, and related domain components.</objective>
<context>
@.planning/phases/04-color-sweep/04-PATTERNS.md
@frontend/src/components/rag
@frontend/src/components/extract
</context>
<tasks>
<task type="auto">
  <name>Task 1: Sweep RAG pages and components</name>
  <files>frontend/src/app/rag/**, frontend/src/components/rag/**</files>
  <action>Replace raw status badges, icon colors, progress fills, destructive hovers, and skeleton grays with semantic classes/components.</action>
  <acceptance_criteria>
    - Scoped color grep over RAG paths returns no planned color hits.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Sweep Extract components</name>
  <files>frontend/src/app/extract/**, frontend/src/components/extract/**</files>
  <action>Replace upload dropzone, settings warning, template manager, and document processor hardcoded colors with semantic tokens.</action>
  <acceptance_criteria>
    - Scoped color grep over Extract paths returns no planned color hits.
  </acceptance_criteria>
</task>
</tasks>
<verification>
- [ ] `cd frontend && npm run lint`
- [ ] scoped color greps for owned paths return zero planned hits
</verification>
