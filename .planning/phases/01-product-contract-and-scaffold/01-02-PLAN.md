---
phase: 01-product-contract-and-scaffold
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/app/services/workflows/__init__.py
  - backend/app/services/workflows/registry.py
  - backend/app/services/workflows/service.py
  - backend/app/services/workflows/templates.py
  - backend/app/modules/workflow/main.py
  - backend/app/modules/workflow/__init__.py
  - backend/tests/unit/services/test_workflow_contracts.py
autonomous: true
requirements: [WF-UX-01, WF-UX-02, WF-DATA-05]
must_haves:
  truths:
    - "A workflow service facade exposes validation, template listing, and step catalog lookup."
    - "A step registry shell declares MVP step capabilities without executing steps."
    - "Template seeds exist for the first three planned workflow use cases."
    - "The existing workflow module remains compatible with current module smoke behavior."
---

<objective>Add service interfaces, step registry shell, template seeds, and workflow module adapter integration points.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add registry and templates</name>
  <files>backend/app/services/workflows/registry.py, backend/app/services/workflows/templates.py, backend/app/services/workflows/__init__.py</files>
  <read_first>
    - .planning/WORKFLOWS_IMPLEMENTATION_PLAN.md
    - backend/app/schemas/workflow.py
  </read_first>
  <action>Create a `StepRegistry` and default catalog entries for `rag.query`, `agent.run`, `notify.in_app`, and `condition.no_results_skip`. Add template seed definitions for Nightly RAG Summary, Connector Intake Triage, and Weekly Extraction Report.</action>
  <acceptance_criteria>
    - Registry exposes step catalog entries by type.
    - Template definitions validate against `WorkflowDefinitionDocument`.
    - Templates are returned as copies so callers cannot mutate shared constants.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Add service facade and module adapter integration</name>
  <files>backend/app/services/workflows/service.py, backend/app/modules/workflow/main.py, backend/app/modules/workflow/__init__.py</files>
  <read_first>
    - backend/app/modules/workflow/main.py
    - backend/app/services/base_module.py
    - backend/app/modules/protocols.py
  </read_first>
  <action>Add a lightweight `WorkflowService` facade for validation, template listing, and step catalog lookup. Wire it into `WorkflowModule` for stats and optional status/catalog/template actions while preserving existing status and execute behavior.</action>
  <acceptance_criteria>
    - Existing workflow module `status` and `execute` actions continue working.
    - Workflow module stats include scaffold catalog/template counts.
    - Focused tests cover registry, templates, and module compatibility.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `cd backend && pytest tests/unit/services/test_workflow_contracts.py tests/test_modules.py::TestModuleIntegration::test_module_dependencies tests/test_modules.py::TestModuleAPI::test_modules_api_response_structure`
</verification>

<success_criteria>
- Phase 2 can build persistence and APIs on stable contracts.
- The module adapter is ready to delegate future production behavior to services.
- No database or scheduler behavior is introduced in Phase 1.
</success_criteria>

## Artifacts This Phase Produces

- `app.services.workflows` scaffold package.
- Workflow module service facade integration.
