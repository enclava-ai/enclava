---
phase: 01-product-contract-and-scaffold
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/workflow.py
  - backend/tests/unit/services/test_workflow_contracts.py
autonomous: true
requirements: [WF-UX-01, WF-UX-02, WF-DATA-05]
must_haves:
  truths:
    - "Workflow definition schemas validate trigger, runtime policy, and ordered typed steps."
    - "Representative workflow samples can be parsed without requiring persistence or execution."
    - "Invalid workflow definitions fail before downstream services attempt execution."
---

<objective>Add workflow domain schemas, runtime policy types, state enums, and sample-definition validation.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add workflow schema contracts</name>
  <files>backend/app/schemas/workflow.py</files>
  <read_first>
    - backend/app/modules/workflow/main.py
    - backend/app/modules/workflow/module.yaml
    - backend/app/schemas/usage_stats.py
    - backend/app/modules/extract/schemas.py
  </read_first>
  <action>Create Pydantic models and enums for workflow definition status, version status, trigger types, run states, step states, runtime policy, retry policy, trigger definition, step definition, and workflow definition document.</action>
  <acceptance_criteria>
    - Contract models import cleanly from `app.schemas.workflow`.
    - Step keys are unique and stable.
    - Step dependencies can only reference earlier steps in the v1 linear sequence.
    - Schedule triggers require cron and timezone.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Add focused schema validation tests</name>
  <files>backend/tests/unit/services/test_workflow_contracts.py</files>
  <read_first>
    - backend/tests/test_modules.py
    - backend/app/schemas/workflow.py
  </read_first>
  <action>Add tests for valid Nightly RAG Summary shape, duplicate step keys, invalid dependency order, and missing schedule fields.</action>
  <acceptance_criteria>
    - Targeted pytest for workflow contracts exits 0.
    - Tests prove invalid definitions are rejected before execution exists.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `cd backend && pytest tests/unit/services/test_workflow_contracts.py`
</verification>

<success_criteria>
- Workflow domain contracts exist and are reusable by later services/API phases.
- The contracts encode the MVP shape from the workflow implementation plan.
- No shipped runtime behavior changes.
</success_criteria>

## Artifacts This Phase Produces

- `app.schemas.workflow` contract module.
