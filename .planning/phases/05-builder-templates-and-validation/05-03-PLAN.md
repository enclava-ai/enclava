---
phase: 05-builder-templates-and-validation
plan: "03"
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - backend/app/services/workflows/templates.py
  - backend/tests/unit/services/test_workflow_builder_api.py
  - frontend/src/components/workflows/WorkflowTemplatePicker.tsx
  - frontend/src/components/workflows/WorkflowTemplatesPanel.tsx
  - frontend/src/components/workflows/WorkflowBuilder.tsx
  - frontend/src/lib/api-client.ts
autonomous: true
requirements: [WF-UX-04, WF-BUILD-02, WF-BUILD-03, WF-BUILD-04]
must_haves:
  truths:
    - "D-04: Template picker can seed a draft from Nightly RAG Summary while Connector Intake Triage and Weekly Extraction Report remain visible but unavailable until Phase 6."
    - "D-05: Nightly template authoring uses workflow lifecycle APIs instead of generic module execution."
    - "D-07: Nightly RAG Summary authoring exposes schedule, RAG collection, agent, recipients, and budget controls with schedule preview."
    - "D-08: Template picker and Nightly authoring stay compact and operational, not marketing-style."
    - "A Nightly RAG Summary draft can validate, publish, enable, and run manually from the builder path."
---

<objective>Add template-assisted authoring and complete the Nightly RAG Summary builder path end to end.</objective>

<threat_model>
<threat severity="medium">Template placeholders could be saved literally and cause broken runtime behavior.</threat>
<mitigation>Replace Nightly template placeholders through explicit builder fields before validation and block publish if placeholders remain in required runtime fields.</mitigation>
<threat severity="medium">Future connector/extract templates could imply support for step handlers that do not exist yet.</threat>
<mitigation>Show those templates as unavailable for publish/run until catalog entries are enabled in Phase 6.</mitigation>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Add template metadata for builder availability</name>
  <files>backend/app/services/workflows/templates.py, backend/tests/unit/services/test_workflow_builder_api.py</files>
  <read_first>
    - backend/app/services/workflows/templates.py
    - backend/app/services/workflows/registry.py
    - backend/tests/unit/services/test_workflow_builder_api.py
  </read_first>
  <action>Add template metadata that identifies `required_placeholders`, `builder_category`, and `available_for_authoring` for each seed. Nightly RAG Summary must be available. Connector Intake Triage and Weekly Extraction Report must remain listed with `available_for_authoring=False` or equivalent metadata until Phase 6 step handlers exist. Tests must assert the metadata is present through the template detail endpoint.</action>
  <acceptance_criteria>
    - Nightly template metadata lists `collection_id`, `agent_id`, and `owner_user_id` placeholders.
    - Connector template metadata marks authoring unavailable because `connector.sync` is disabled.
    - Weekly Extraction template metadata marks authoring unavailable because `extract.run_template` is disabled.
    - Template detail endpoint serializes metadata.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Build template picker and operations template actions</name>
  <files>frontend/src/components/workflows/WorkflowTemplatePicker.tsx, frontend/src/components/workflows/WorkflowTemplatesPanel.tsx, frontend/src/lib/api-client.ts</files>
  <read_first>
    - frontend/src/components/workflows/WorkflowTemplatesPanel.tsx
    - frontend/src/components/workflows/WorkflowBuilder.tsx
    - frontend/src/lib/api-client.ts
    - frontend/src/components/ui/status-badge.tsx
  </read_first>
  <action>Create `WorkflowTemplatePicker` that loads the full template catalog and renders compact template cards with trigger type, step count, tags, and availability. Update the Templates tab so available templates link to `/workflows/new?template=nightly-rag-summary`; unavailable templates show a disabled badge and no create action. The picker must not use nested cards or marketing-style hero copy.</action>
  <acceptance_criteria>
    - Nightly RAG Summary card has a Create action linking to `/workflows/new?template=nightly-rag-summary`.
    - Connector Intake Triage card shows unavailable or Phase 6 status.
    - Weekly Extraction Report card shows unavailable or Phase 6 status.
    - `npm run check:plumbing` passes with no direct client `fetch` in the new picker.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 3: Add Nightly RAG Summary authoring fields</name>
  <files>frontend/src/components/workflows/WorkflowBuilder.tsx, frontend/src/components/workflows/WorkflowStepProperties.tsx, frontend/src/lib/api-client.ts</files>
  <read_first>
    - frontend/src/components/workflows/WorkflowBuilder.tsx
    - frontend/src/components/workflows/WorkflowStepProperties.tsx
    - frontend/src/lib/api-client.ts
    - frontend/src/app/api/rag/collections/route.ts
  </read_first>
  <action>When `template=nightly-rag-summary` is present, load the full template, seed builder state, and expose first-class controls for schedule cron/timezone, RAG collection, query, agent, recipients, notification copy, retry attempts, concurrency policy, redaction policy, and budget limit. Use existing `agentApi.listAgents` for agent options and add a small `ragApi.listCollections` helper using `/api/rag/collections` for collection options. Convert selected values into the existing template step configs before validation/save.</action>
  <acceptance_criteria>
    - Template-seeded draft contains steps `find_new_docs`, `skip_if_empty`, `summarize`, and `notify_owner`.
    - Required placeholders are replaced before `workflowApi.validateDefinition` is called.
    - Validation blocks publish if collection, agent, or recipient values are missing.
    - Schedule preview shows five upcoming times for the selected cron/timezone.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 4: Verify Nightly publish, enable, and run path</name>
  <files>backend/tests/unit/services/test_workflow_builder_api.py, frontend/src/components/workflows/WorkflowBuilder.tsx</files>
  <read_first>
    - backend/tests/unit/services/test_workflow_run_api.py
    - backend/tests/unit/services/test_workflow_step_handlers.py
    - frontend/src/components/workflows/WorkflowBuilder.tsx
  </read_first>
  <action>Add backend test coverage that creates a Nightly RAG Summary definition from template-shaped data, validates it, creates a draft, publishes it, enables it, and starts a manual run with fake RAG and agent dependencies where required. In the UI, after publish/enable succeeds, show actions to open the workflow in operations and run now when safe.</action>
  <acceptance_criteria>
    - Backend test asserts Nightly template-shaped payload publishes and enables.
    - Backend test asserts manual run creation returns a run id.
    - Builder publish success state includes link to `/workflows`.
    - Run-now action is not shown until the workflow is published and active.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test pytest --no-cov -q tests/unit/services/test_workflow_builder_api.py tests/unit/services/test_workflow_run_api.py tests/unit/services/test_workflow_step_handlers.py`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test black --check app/services/workflows/templates.py tests/unit/services/test_workflow_builder_api.py`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test isort --check-only app/services/workflows/templates.py tests/unit/services/test_workflow_builder_api.py`
- [ ] `cd frontend && npm run lint`
- [ ] `cd frontend && npm run check:colors`
- [ ] `cd frontend && npm run check:plumbing`
- [ ] `cd frontend && npm run build`
- [ ] `sudo docker compose up -d --build`
- [ ] `curl -fsS http://localhost:1080/health`
- [ ] Authenticated smoke: `/api/workflows?resource=template&template_id=nightly-rag-summary`
</verification>

<success_criteria>
- Users can start from the Nightly RAG Summary template and author it without JSON editing.
- Future connector/extract templates are discoverable without pretending to be executable.
- Nightly template path validates, publishes, enables, and starts a manual run.
</success_criteria>

## Artifacts This Plan Produces

- `WorkflowTemplatePicker`
- `ragApi.listCollections`
- Nightly template builder state mapper
- Template availability metadata
- Nightly template publish/enable/run tests
