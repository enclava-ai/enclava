---
phase: 06-connector-and-extract-integration
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - backend/app/modules/extract/services/extract_service.py
  - backend/app/services/workflows/registry.py
  - backend/app/services/workflows/steps.py
  - backend/app/services/workflows/templates.py
  - backend/tests/unit/services/test_workflow_extract_steps.py
  - backend/tests/unit/services/test_workflow_builder_api.py
  - frontend/src/lib/api-client.ts
  - frontend/src/components/workflows/WorkflowBuilder.tsx
  - frontend/src/components/workflows/WorkflowStepProperties.tsx
  - frontend/src/components/workflows/WorkflowRunTimeline.tsx
autonomous: true
requirements: [WF-INT-02, WF-INT-03, WF-RUN-04, WF-SEC-04]
must_haves:
  truths:
    - "Extract execution is a workflow step over selected existing documents, not an upload-only UI shortcut."
    - "Extract outputs are stored as workflow artifacts and linked to Extract job/result records."
    - "Weekly Extraction Report becomes authorable only after extract.run_template runtime and builder support are enabled."
    - "Extract failures and validation issues are visible from workflow run detail."
    - "Containers are rebuilt and the live stack is smoked after implementation."
---

<objective>Add Extract template workflow execution, artifact previews, and Weekly Extraction Report authoring/run support.</objective>

<threat_model>
<threat severity="medium">Extraction outputs may contain sensitive document data.</threat>
<mitigation>Use existing workflow redaction policy for artifact data and avoid adding unredacted side channels in events or UI labels.</mitigation>
<threat severity="medium">A workflow could extract too many documents in one step and create excessive model cost.</threat>
<mitigation>Add backend max document/output limits, expose compact builder limits, and preserve workflow budget checks/cost accounting.</mitigation>
<threat severity="medium">Invalid Extract template context could fail late after model spend.</threat>
<mitigation>Validate context JSON and required document-source fields before execution/publish when possible.</mitigation>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Add workflow-facing Extract adapter</name>
  <files>backend/app/modules/extract/services/extract_service.py, backend/tests/unit/services/test_workflow_extract_steps.py</files>
  <read_first>
    - backend/app/modules/extract/services/extract_service.py
    - backend/app/modules/extract/templates/manager.py
    - backend/app/models/extract_job.py
    - backend/app/models/extract_result.py
    - backend/app/models/rag_document.py
  </read_first>
  <action>Add a workflow-facing adapter that can run an Extract template over existing document text summaries. It must load the active template, create an `ExtractJob`, create a final `ExtractResult`, call the LLM path when a real service is used, and support dependency injection/fakes in tests. The adapter should accept document summaries, context, current actor, workflow run id, and step key.</action>
  <acceptance_criteria>
    - Adapter returns `job_id`, `template_id`, `status`, `document_count`, `result`, validation issues, token/cost data, and summary.
    - Adapter persists `ExtractJob` and `ExtractResult` for successful execution.
    - Adapter marks job failed with actionable error text when template or model execution fails.
    - Unit tests can run without real external LLM credentials by injecting a fake extract service/dependency.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Enable extract.run_template in workflow registry and runtime</name>
  <files>backend/app/services/workflows/registry.py, backend/app/services/workflows/steps.py, backend/tests/unit/services/test_workflow_extract_steps.py</files>
  <read_first>
    - backend/app/services/workflows/registry.py
    - backend/app/services/workflows/steps.py
    - backend/app/services/workflows/runtime.py
    - backend/app/services/workflows/service.py
  </read_first>
  <action>Flip `extract.run_template` to enabled and implement `ExtractRunTemplateHandler`. The handler must resolve documents from either a previous step output or a RAG document filter, validate required config, call `dependencies.extract_service` when supplied or the Extract workflow adapter otherwise, return compact output data, create an `extract_result` artifact plus optional JSON artifact, and emit extract events.</action>
  <acceptance_criteria>
    - `create_default_step_registry().get("extract.run_template").enabled is True`.
    - Missing `template_id` validation returns `steps[n].config.template_id`.
    - Missing/invalid document source returns an actionable validation or runtime error.
    - Handler output includes `summary`, `job_id`, `document_count`, and `result`.
    - Artifact list includes an Extract artifact tied to the step run.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 3: Make Weekly Extraction Report authorable</name>
  <files>backend/app/services/workflows/templates.py, backend/tests/unit/services/test_workflow_builder_api.py</files>
  <read_first>
    - backend/app/services/workflows/templates.py
    - backend/app/services/workflows/service.py
    - backend/tests/unit/services/test_workflow_builder_api.py
  </read_first>
  <action>Mark Weekly Extraction Report `available_for_authoring=True`, update seed definition config to match the implemented document-source schema, and keep raw placeholder validation strict. Add tests for raw placeholder rejection and filled definition validate/publish/enable/manual run creation.</action>
  <acceptance_criteria>
    - Template catalog reports Weekly Extraction Report as available.
    - Raw weekly template validation rejects unresolved placeholders.
    - Filled weekly template validation succeeds with `extract.run_template` and `notify.in_app`.
    - Connector Intake Triage remains available after Plan 06-02.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 4: Add Extract builder UI and artifact previews</name>
  <files>frontend/src/lib/api-client.ts, frontend/src/components/workflows/WorkflowBuilder.tsx, frontend/src/components/workflows/WorkflowStepProperties.tsx, frontend/src/components/workflows/WorkflowRunTimeline.tsx</files>
  <read_first>
    - frontend/src/lib/api-client.ts
    - frontend/src/components/workflows/WorkflowBuilder.tsx
    - frontend/src/components/workflows/WorkflowStepProperties.tsx
    - frontend/src/components/workflows/WorkflowRunTimeline.tsx
    - frontend/src/components/extract/TemplateManager.tsx
  </read_first>
  <action>Add typed Extract template option loading to the builder, seed extract template steps with blank template id and safe document-source defaults, add default config/key mapping for `extract.run_template`, and add `ExtractTemplateEditor` with template select, document source, previous step/collection controls, optional connector filter, since mode, max documents, context JSON, and retry attempts. Enhance artifact list previews for JSON/extract artifacts using the existing redacted payload shape.</action>
  <acceptance_criteria>
    - Enabled `extract.run_template` appears in the Add step menu after backend catalog support.
    - Weekly Extraction Report opens in `/workflows/new?template=weekly-extraction-report` with template id blank and validation-ready fields.
    - Artifact previews are bounded and do not overflow mobile or desktop layouts.
    - No new one-off workflow API plumbing bypasses `api-client.ts`/Next proxy conventions.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test pytest --no-cov -q tests/unit/services/test_workflow_extract_steps.py tests/unit/services/test_workflow_connector_steps.py tests/unit/services/test_workflow_builder_api.py tests/unit/services/test_workflow_step_handlers.py tests/unit/services/test_workflow_run_api.py`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test black --check app/modules/extract/services/extract_service.py app/services/workflows/registry.py app/services/workflows/steps.py app/services/workflows/templates.py tests/unit/services/test_workflow_extract_steps.py tests/unit/services/test_workflow_builder_api.py`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test isort --check-only app/modules/extract/services/extract_service.py app/services/workflows/registry.py app/services/workflows/steps.py app/services/workflows/templates.py tests/unit/services/test_workflow_extract_steps.py tests/unit/services/test_workflow_builder_api.py`
- [ ] `cd frontend && npm run lint`
- [ ] `cd frontend && npm run check:colors`
- [ ] `cd frontend && npm run check:plumbing`
- [ ] `cd frontend && npm run build`
- [ ] `git diff --check -- . ':!CLAUDE.md' ':!design-proposal'`
- [ ] `sudo docker compose up -d --build`
- [ ] `sudo docker compose up -d --force-recreate enclava-nginx`
- [ ] `curl -fsS http://localhost:1080/health`
- [ ] Authenticated smoke: extract catalog entry enabled, Weekly Extraction template available, filled weekly definition validates, artifact preview route renders.
</verification>

<success_criteria>
- `extract.run_template` can execute as a workflow step over selected existing documents.
- Extract results are visible as workflow artifacts and tied to Extract job/result records.
- Weekly Extraction Report can be authored from template and run through the workflow runtime with fake or real Extract dependencies.
- Extract failures and validation issues are visible from workflow run detail.
</success_criteria>

## Artifacts This Plan Produces

- Extract workflow execution adapter/result
- `ExtractRunTemplateHandler`
- Enabled `extract.run_template` catalog entry
- Weekly Extraction Report available template
- Extract step property editor
- Extract artifact preview in workflow run detail
