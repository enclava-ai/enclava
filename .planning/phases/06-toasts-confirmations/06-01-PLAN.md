---
phase: 06-toasts-confirmations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/layout.tsx
  - frontend/src/hooks/use-toast.ts
  - frontend/src/components/ui/toaster.tsx
  - frontend/src/app/settings/llm/page.tsx
  - frontend/src/components/llm/UsageTab.tsx
  - frontend/src/app/admin/users/page.tsx
  - frontend/src/app/admin/connectors/page.tsx
  - frontend/package.json
  - frontend/package-lock.json
autonomous: true
requirements: [FDBK-01, FDBK-02]
user_setup: []
must_haves:
  truths:
    - "Only the selected shadcn-style toast provider is mounted."
    - "`@/hooks/use-toast` callers share provider state with the mounted `Toaster`."
    - "No `react-hot-toast` or `sonner` imports/dependencies remain."
---

<objective>Consolidate toast providers, dependencies, and third-party toast call sites.</objective>
<context>
@.planning/phases/06-toasts-confirmations/06-CONTEXT.md
@.planning/phases/06-toasts-confirmations/06-RESEARCH.md
@.planning/phases/06-toasts-confirmations/06-PATTERNS.md
@.planning/phases/06-toasts-confirmations/06-VALIDATION.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Make the selected toast API truly shared</name>
  <files>frontend/src/hooks/use-toast.ts, frontend/src/components/ui/toaster.tsx, frontend/src/app/layout.tsx</files>
  <action>Route `@/hooks/use-toast` through the shared `ToastContext`, keep one shadcn-style `Toaster`, and remove `HotToaster`/`Sonner` providers from layout.</action>
  <acceptance_criteria>
    - `Toaster` and all `@/hooks/use-toast` callers read the same provider state.
    - Layout mounts only one toast provider/toaster system.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Migrate third-party toast call sites and dependencies</name>
  <files>frontend/src/app/settings/llm/page.tsx, frontend/src/components/llm/UsageTab.tsx, frontend/src/app/admin/users/page.tsx, frontend/src/app/admin/connectors/page.tsx, frontend/package.json, frontend/package-lock.json</files>
  <action>Replace `toast.success/error` calls from `react-hot-toast` and `sonner` with object-style `useToast` calls, then uninstall unused toast packages.</action>
  <acceptance_criteria>
    - Toast scan from `06-VALIDATION.md` returns no third-party toast matches.
  </acceptance_criteria>
</task>
</tasks>
<verification>
- [ ] toast scan from `06-VALIDATION.md` returns no matches
- [ ] `cd frontend && npm run lint`
- [ ] `cd frontend && npm run build`
</verification>
<success_criteria>
- One toast system is mounted.
- Third-party toast dependencies are removed.
- Converted call sites preserve success/error messaging.
</success_criteria>
