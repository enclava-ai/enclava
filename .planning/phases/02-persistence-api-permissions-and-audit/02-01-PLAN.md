---
phase: 02-persistence-api-permissions-and-audit
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/workflow.py
  - backend/app/models/__init__.py
  - backend/alembic/versions/034_add_workflow_lifecycle.py
  - backend/tests/unit/services/test_workflow_persistence.py
autonomous: true
requirements: [WF-DATA-01, WF-DATA-02, WF-DATA-03]
must_haves:
  truths:
    - "Workflow lifecycle storage is additive and migration-safe against the existing legacy workflow tables."
    - "Published workflow versions are represented as immutable rows separate from mutable definitions."
    - "Triggers, runs, step runs, artifacts, and events have durable model/table contracts for later runtime phases."
---

<objective>Add workflow persistence models and migration.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add workflow SQLAlchemy models</name>
  <files>backend/app/models/workflow.py, backend/app/models/__init__.py</files>
  <read_first>
    - backend/app/models/connector_source.py
    - backend/app/models/audit_log.py
    - backend/app/db/database.py
    - backend/app/schemas/workflow.py
  </read_first>
  <action>Create SQLAlchemy models for workflow definitions, versions, triggers, runs, step runs, artifacts, and events. Preserve legacy `workflow_definitions` columns by mapping them safely while adding lifecycle fields needed by v1.1.</action>
  <acceptance_criteria>
    - Models import cleanly from `app.models`.
    - `workflow_definitions` can coexist with legacy columns from migration `000`.
    - Relationships support definition -> versions/triggers/runs and run -> steps/artifacts/events.
    - JSON columns work under both Postgres and SQLite test metadata.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Add additive Alembic migration</name>
  <files>backend/alembic/versions/034_add_workflow_lifecycle.py</files>
  <read_first>
    - backend/alembic/versions/000_consolidated_ground_truth_schema.py
    - backend/alembic/versions/032_add_connector_sources.py
    - backend/alembic/versions/033_remove_chatbots_and_zammad.py
  </read_first>
  <action>Add an Alembic migration that extends `workflow_definitions` with lifecycle/owner/published-version fields and creates `workflow_versions`, `workflow_triggers`, `workflow_runs`, `workflow_step_runs`, `workflow_artifacts`, and `workflow_events`.</action>
  <acceptance_criteria>
    - Migration revises `033_remove_chatbots`.
    - Migration does not recreate or drop existing legacy workflow tables.
    - Indexes support list/detail queries by owner, status, workflow id, version id, run status, and timestamps.
    - Downgrade removes only the fields/tables introduced by this migration.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 3: Add persistence model tests</name>
  <files>backend/tests/unit/services/test_workflow_persistence.py</files>
  <read_first>
    - backend/tests/conftest.py
    - backend/tests/unit/services/test_workflow_contracts.py
  </read_first>
  <action>Add focused tests that create workflow model instances, assert model defaults, and verify version snapshots and trigger rows can be associated without executing a workflow.</action>
  <acceptance_criteria>
    - Focused tests pass in the backend test container.
    - Tests prove definition/version relationships and immutable snapshot fields exist.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test pytest --no-cov tests/unit/services/test_workflow_persistence.py`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test alembic upgrade head`
</verification>

<success_criteria>
- Durable workflow table/model contracts exist for lifecycle and future runtime phases.
- The schema change is safe for existing databases that already contain legacy workflow tables.
- No workflow execution or scheduling behavior is introduced.
</success_criteria>

## Artifacts This Phase Produces

- Workflow SQLAlchemy model module.
- Additive workflow lifecycle Alembic migration.
