---
phase: 02-persistence-api-permissions-and-audit
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - backend/app/schemas/workflow.py
  - backend/app/services/workflows/service.py
  - backend/app/services/workflows/__init__.py
  - backend/tests/unit/services/test_workflow_lifecycle_service.py
autonomous: true
requirements: [WF-DATA-02, WF-DATA-03, WF-SEC-01]
must_haves:
  truths:
    - "Workflow lifecycle service validates draft definitions before publish."
    - "Published versions are immutable and historical version rows are never mutated by later draft edits."
    - "Create, update, publish, enable, disable, archive, list, and detail enforce authenticated actor context."
---

<objective>Implement workflow lifecycle service, validation, publish/version semantics, and permission checks.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add API/service schemas</name>
  <files>backend/app/schemas/workflow.py</files>
  <read_first>
    - backend/app/schemas/workflow.py
    - backend/app/schemas/audit.py
    - backend/app/api/v1/connectors.py
  </read_first>
  <action>Add request and response schemas for workflow create, update, list item, detail, version summary, lifecycle actions, and validation errors.</action>
  <acceptance_criteria>
    - Request schemas reuse `WorkflowDefinitionDocument` instead of duplicating step/trigger validation.
    - Response schemas serialize datetimes and enum-like status values consistently.
    - Mutating request payloads do not expose raw secret fields.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Extend WorkflowService with lifecycle operations</name>
  <files>backend/app/services/workflows/service.py, backend/app/services/workflows/__init__.py</files>
  <read_first>
    - backend/app/services/workflows/service.py
    - backend/app/models/workflow.py
    - backend/app/core/security.py
    - backend/app/services/audit_service.py
  </read_first>
  <action>Implement async service methods for create, update draft, validate, publish, enable, disable, archive, list, and detail. Add permission helper behavior that allows owners/admins to read and allows admins/superusers or explicit workflow manage permission to mutate.</action>
  <acceptance_criteria>
    - `publish` creates a new immutable `WorkflowVersion` with an incremented version number.
    - `enable` fails clearly if no published version exists.
    - `archive` hides workflows from default list results without deleting rows.
    - Service raises typed not-found/permission/validation errors that APIs can map to HTTP status codes.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 3: Add lifecycle service tests</name>
  <files>backend/tests/unit/services/test_workflow_lifecycle_service.py</files>
  <read_first>
    - backend/tests/conftest.py
    - backend/tests/unit/services/test_workflow_contracts.py
  </read_first>
  <action>Add async tests for create/update/publish/version immutability, enable without publish rejection, owner/admin list filtering, and archive hiding.</action>
  <acceptance_criteria>
    - Service tests pass in Docker with coverage disabled.
    - Tests cover both owner and admin permission paths.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test pytest --no-cov tests/unit/services/test_workflow_lifecycle_service.py tests/unit/services/test_workflow_contracts.py`
</verification>

<success_criteria>
- Lifecycle behavior is available to APIs and future UI without going through the generic module execute endpoint.
- Version immutability and permission rules are covered by tests.
</success_criteria>

## Artifacts This Phase Produces

- Workflow lifecycle service methods.
- Workflow lifecycle request/response schemas.
