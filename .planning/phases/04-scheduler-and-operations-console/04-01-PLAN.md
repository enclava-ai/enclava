---
phase: 04-scheduler-and-operations-console
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/app/schemas/workflow.py
  - backend/app/services/workflows/scheduler.py
  - backend/app/services/workflows/runtime.py
  - backend/app/services/workflows/__init__.py
  - backend/app/api/internal_v1/workflows.py
  - backend/app/tasks/workflow_scheduler.py
  - backend/app/main.py
  - backend/tests/unit/services/test_workflow_scheduler.py
  - backend/tests/unit/services/test_workflow_scheduler_api.py
autonomous: true
requirements: [WF-SCHED-01, WF-SCHED-02, WF-SCHED-03, WF-RUN-02, WF-SEC-02]
must_haves:
  truths:
    - "Cron plus IANA timezone schedules can be previewed before publish."
    - "Enabled schedule triggers receive a durable `next_run_at`."
    - "Due scheduled runs are created idempotently and never duplicated across repeated ticks."
    - "An in-process scheduler/worker loop starts and stops with FastAPI lifespan."
    - "Misfire and concurrency policies are enforced at due-run creation time."
---

<objective>Implement backend schedule preview, due-run creation, idempotency, misfire/concurrency policy, and the lightweight scheduler/worker loop.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add schedule contracts and dependency</name>
  <files>backend/requirements.txt, backend/app/schemas/workflow.py</files>
  <read_first>
    - backend/requirements.txt
    - backend/app/schemas/workflow.py
    - backend/tests/unit/services/test_workflow_contracts.py
  </read_first>
  <action>Add `croniter` as the cron calculation dependency and define schedule preview, scheduler tick, due-run, and scheduler status response schemas. Keep existing trigger schemas compatible.</action>
  <acceptance_criteria>
    - Invalid cron expressions produce structured validation errors.
    - Invalid IANA timezones produce structured validation errors.
    - Preview responses include UTC timestamps and the requested timezone.
    - Existing workflow contract tests still pass.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Implement schedule calculator</name>
  <files>backend/app/services/workflows/scheduler.py</files>
  <read_first>
    - backend/app/db/database.py
    - backend/app/schemas/workflow.py
    - backend/app/models/workflow.py
  </read_first>
  <action>Create a scheduler service with cron/timezone validation, next-run calculation, preview generation, and trigger `next_run_at` recalculation helpers using naive UTC datetimes for database writes.</action>
  <acceptance_criteria>
    - Preview can return the next 5 fire times for daily and weekly schedules.
    - Daylight-saving-sensitive timezones are handled through `zoneinfo`.
    - Database writes remain naive UTC per the existing database convention.
    - Unit tests freeze `now` values and do not depend on wall-clock timing.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 3: Create scheduled due runs idempotently</name>
  <files>backend/app/services/workflows/scheduler.py, backend/app/services/workflows/runtime.py</files>
  <read_first>
    - backend/app/services/workflows/runtime.py
    - backend/app/services/workflows/service.py
    - backend/app/models/workflow.py
  </read_first>
  <action>Add due-trigger polling that locks enabled due schedule triggers, creates queued `WorkflowRun` records with schedule idempotency keys, updates `last_fire_at` and `next_run_at`, emits events/audit rows, and enforces concurrency/misfire policy.</action>
  <acceptance_criteria>
    - Re-running the same tick does not create duplicate runs for the same trigger fire time.
    - `skip_if_running` skips due creation when the workflow has queued/running/paused runs.
    - `queue_after_current` and `allow_parallel` create queued runs with clear idempotency keys.
    - `run_once`, `skip`, and `catch_up` misfire policies are covered by tests.
    - Trigger rows are advanced to a future `next_run_at` after processing.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 4: Add in-process scheduler and worker loop</name>
  <files>backend/app/tasks/workflow_scheduler.py, backend/app/main.py</files>
  <read_first>
    - backend/app/tasks/connector_sync.py
    - backend/app/main.py
    - backend/app/services/workflows/runtime.py
  </read_first>
  <action>Add a module-level asyncio scheduler that starts from FastAPI lifespan, ticks due scheduled runs, then claims and executes a bounded number of queued runs. Stop it cleanly on shutdown.</action>
  <acceptance_criteria>
    - Scheduler start/stop mirrors connector scheduler behavior.
    - Tick failures are logged and do not kill the loop.
    - Worker uses `WorkflowRuntimeService.execute_run` so run history matches manual execution.
    - Batch limits prevent one tick from monopolizing the event loop.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 5: Add scheduler internal APIs</name>
  <files>backend/app/api/internal_v1/workflows.py</files>
  <read_first>
    - backend/app/api/internal_v1/workflows.py
    - backend/tests/unit/services/test_workflow_api.py
    - backend/tests/unit/services/test_workflow_run_api.py
  </read_first>
  <action>Add internal endpoints for schedule preview, scheduler status, and a protected manual scheduler tick for tests/operators.</action>
  <acceptance_criteria>
    - Routes live under `/api-internal/v1/workflows`.
    - Preview route requires auth and returns structured validation failures.
    - Manual tick route requires manage/admin-style workflow permission and returns created/skipped/executed counts.
    - Existing lifecycle and run APIs keep their route ordering and behavior.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 6: Add scheduler tests</name>
  <files>backend/tests/unit/services/test_workflow_scheduler.py, backend/tests/unit/services/test_workflow_scheduler_api.py</files>
  <read_first>
    - backend/tests/conftest.py
    - backend/tests/unit/services/test_workflow_runtime.py
    - backend/tests/unit/services/test_workflow_run_api.py
  </read_first>
  <action>Add focused tests for preview, next-run calculation, enabled trigger next_run_at, due-run idempotency, concurrency policy, misfire policy, worker execution, API permissions, and route responses.</action>
  <acceptance_criteria>
    - Tests pass in Docker without live external providers.
    - Tests prove duplicate due ticks do not duplicate workflow runs.
    - Tests prove scheduled queued runs execute through the same step/runtime history path.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test pytest --no-cov -q tests/unit/services/test_workflow_scheduler.py tests/unit/services/test_workflow_scheduler_api.py tests/unit/services/test_workflow_runtime.py tests/unit/services/test_workflow_run_api.py tests/unit/services/test_workflow_lifecycle_service.py tests/unit/services/test_workflow_contracts.py`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test black --check app/schemas/workflow.py app/services/workflows app/api/internal_v1/workflows.py app/tasks/workflow_scheduler.py tests/unit/services/test_workflow_scheduler.py tests/unit/services/test_workflow_scheduler_api.py`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test isort --check-only app/schemas/workflow.py app/services/workflows app/api/internal_v1/workflows.py app/tasks/workflow_scheduler.py tests/unit/services/test_workflow_scheduler.py tests/unit/services/test_workflow_scheduler_api.py`
- [ ] `sudo docker compose -f docker-compose.test.yml build enclava-backend-test`
</verification>

<success_criteria>
- Scheduled triggers can be previewed and advanced to future run times.
- Due scheduled runs are created exactly once per fire time.
- The app has a lightweight scheduler/worker loop ready for operations UI visibility.
</success_criteria>

## Artifacts This Plan Produces

- Scheduler service and tests.
- Workflow scheduler background task.
- Internal schedule preview/tick APIs.
