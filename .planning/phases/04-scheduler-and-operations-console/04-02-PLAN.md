---
phase: 04-scheduler-and-operations-console
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - backend/app/schemas/workflow.py
  - backend/app/services/workflows/operations.py
  - backend/app/api/internal_v1/workflows.py
  - backend/tests/unit/services/test_workflow_operations_api.py
  - frontend/src/components/ui/navigation.tsx
  - frontend/src/lib/api-client.ts
  - frontend/src/app/api/workflows/route.ts
  - frontend/src/app/workflows/page.tsx
  - frontend/src/components/workflows/WorkflowOperationsConsole.tsx
autonomous: true
requirements: [WF-UX-03, WF-SCHED-04, WF-RUN-03, WF-SEC-03]
must_haves:
  truths:
    - "Primary navigation includes Workflows only after `/workflows` exists."
    - "The Workflows route opens on a health-first operations console, not the builder."
    - "Operations data includes workflow status, health, latest run, active run, next run, failures, owner, tags, and budget metadata."
    - "Run-now and open-run actions work from the console without full page reload."
---

<objective>Build the backend operations summary API and the primary Workflows operations console.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add operations summary service</name>
  <files>backend/app/services/workflows/operations.py, backend/app/schemas/workflow.py</files>
  <read_first>
    - backend/app/models/workflow.py
    - backend/app/services/workflows/service.py
    - backend/app/services/workflows/runtime.py
  </read_first>
  <action>Add operations summary schemas and service queries for workflow list rows, active runs, latest runs, failure counts, next schedule, last successful run, health state, and budget/cost rollups.</action>
  <acceptance_criteria>
    - Owner/admin/workflow.read visibility matches lifecycle APIs.
    - Health states distinguish healthy, disabled, running, failed, missed, and no-schedule states.
    - Queries avoid async lazy-load failures.
    - Data is stable for workflows with no runs yet.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Add operations internal API</name>
  <files>backend/app/api/internal_v1/workflows.py, backend/tests/unit/services/test_workflow_operations_api.py</files>
  <read_first>
    - backend/app/api/internal_v1/workflows.py
    - backend/tests/unit/services/test_workflow_api.py
    - backend/tests/unit/services/test_workflow_scheduler_api.py
  </read_first>
  <action>Add `/operations` and supporting recent-runs/failures endpoints under `/api-internal/v1/workflows`, with tests for visibility, empty state, health state, and run-now integration.</action>
  <acceptance_criteria>
    - API returns a compact payload suitable for the console without N+1 queries.
    - Non-owners cannot see private workflow operations rows.
    - Run-now uses existing manual run API semantics and audit behavior.
    - Existing `/runs/{run_id}` static route ordering remains intact.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 3: Add frontend API proxies and types</name>
  <files>frontend/src/lib/api-client.ts, frontend/src/app/api/workflows/route.ts</files>
  <read_first>
    - frontend/src/lib/api-client.ts
    - frontend/src/app/api/workflows/runs/[runId]/route.ts
    - frontend/src/lib/proxy-auth.ts
  </read_first>
  <action>Add workflow operations types, API client helpers, and a Next.js proxy route that forwards Authorization to backend internal operations APIs.</action>
  <acceptance_criteria>
    - Client code does not call `fetch` directly outside allowed plumbing files.
    - Proxy preserves backend error status codes.
    - Types cover operations rows, run summaries, health states, and action responses.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 4: Build Workflows operations console</name>
  <files>frontend/src/app/workflows/page.tsx, frontend/src/components/workflows/WorkflowOperationsConsole.tsx</files>
  <read_first>
    - frontend/src/app/dashboard/page.tsx
    - frontend/src/components/workflows/WorkflowRunTimeline.tsx
    - frontend/src/components/ui/status-badge.tsx
    - frontend/src/components/ui/table.tsx
    - frontend/src/components/ui/button.tsx
  </read_first>
  <action>Build a compact operations console with workflow list, filters, status/health, next run, latest run, active/failure indicators, budget summary, run-now action, retry/open latest failed run, and empty state using templates as starting points.</action>
  <acceptance_criteria>
    - First screen answers what is running, what failed, and what happens next.
    - UI is dense and dashboard-like, not a landing page.
    - Run-now updates the selected row without a full page reload.
    - Empty state shows template starting points, not marketing copy.
    - Text fits on mobile and desktop.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 5: Add Workflows navigation entry</name>
  <files>frontend/src/components/ui/navigation.tsx</files>
  <read_first>
    - frontend/src/components/ui/navigation.tsx
    - frontend/src/contexts/ModulesContext.tsx
  </read_first>
  <action>Add `workflow: { href: "/workflows", label: "Workflows" }` to the module navigation map once the route exists.</action>
  <acceptance_criteria>
    - Navigation entry appears only when the workflow module is enabled and initialized.
    - Existing RAG and Extract navigation behavior is unchanged.
    - Mobile navigation highlights `/workflows` and nested run detail routes correctly.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test pytest --no-cov -q tests/unit/services/test_workflow_operations_api.py tests/unit/services/test_workflow_scheduler.py tests/unit/services/test_workflow_api.py tests/unit/services/test_workflow_run_api.py`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test black --check app/schemas/workflow.py app/services/workflows app/api/internal_v1/workflows.py tests/unit/services/test_workflow_operations_api.py`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test isort --check-only app/schemas/workflow.py app/services/workflows app/api/internal_v1/workflows.py tests/unit/services/test_workflow_operations_api.py`
- [ ] `cd frontend && npm run lint`
- [ ] `cd frontend && npm run check:colors`
- [ ] `cd frontend && npm run check:plumbing`
- [ ] `cd frontend && npm run build`
</verification>

<success_criteria>
- `/workflows` exists and is the default operational surface.
- Users can see workflow health, next scheduled run, latest run, failures, and run-now action from one screen.
- Workflows appears in primary navigation when the module is enabled.
</success_criteria>

## Artifacts This Plan Produces

- Operations summary service and API.
- Workflows route and operations console.
- Primary navigation entry for the workflow module.
