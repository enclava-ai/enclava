---
phase: 04-scheduler-and-operations-console
plan: "03"
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - backend/app/schemas/workflow.py
  - backend/app/services/workflows/operations.py
  - backend/app/api/internal_v1/workflows.py
  - backend/tests/unit/services/test_workflow_schedule_board_api.py
  - frontend/src/lib/api-client.ts
  - frontend/src/app/workflows/page.tsx
  - frontend/src/components/workflows/WorkflowScheduleBoard.tsx
  - frontend/src/components/workflows/WorkflowRunsTable.tsx
autonomous: true
requirements: [WF-UX-05, WF-SCHED-01, WF-SCHED-03, WF-SCHED-04, WF-SEC-02]
must_haves:
  truths:
    - "Workflows page exposes Overview, Runs, Schedules, and Templates tabs."
    - "Schedule board shows upcoming scheduled runs and schedule health."
    - "Disable/enable controls write lifecycle audit records and refresh visible state."
    - "Schedule preview is available from the schedule board for existing schedule triggers."
---

<objective>Add schedule board, upcoming runs, recent run history, and workflow enable/disable controls to complete the Phase 4 operations UX.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add schedule board API data</name>
  <files>backend/app/services/workflows/operations.py, backend/app/schemas/workflow.py, backend/app/api/internal_v1/workflows.py</files>
  <read_first>
    - backend/app/services/workflows/scheduler.py
    - backend/app/services/workflows/operations.py
    - backend/app/models/workflow.py
  </read_first>
  <action>Add backend data for schedule board groups, upcoming trigger fire times, missed/due/disabled schedule health, recent run history, and template summaries.</action>
  <acceptance_criteria>
    - Upcoming runs are grouped into today, tomorrow, this week, and later.
    - Disabled workflows and disabled triggers appear with clear health state.
    - Recent run history can filter by status and workflow id.
    - Schedule board APIs respect the same visibility rules as operations summary.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Add enable/disable and preview actions to operations API</name>
  <files>backend/app/api/internal_v1/workflows.py, backend/tests/unit/services/test_workflow_schedule_board_api.py</files>
  <read_first>
    - backend/app/api/internal_v1/workflows.py
    - backend/tests/unit/services/test_workflow_api.py
    - backend/tests/unit/services/test_workflow_scheduler_api.py
  </read_first>
  <action>Expose schedule preview for existing triggers and verify enable/disable flows refresh trigger scheduling state while preserving lifecycle audit behavior.</action>
  <acceptance_criteria>
    - Enabling a scheduled workflow computes a fresh future `next_run_at`.
    - Disabling a workflow disables the current trigger.
    - Schedule preview returns next five times for a workflow's current schedule.
    - Tests assert audit rows for enable/disable actions.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 3: Add Workflows page tabs and runs table</name>
  <files>frontend/src/app/workflows/page.tsx, frontend/src/components/workflows/WorkflowRunsTable.tsx, frontend/src/lib/api-client.ts</files>
  <read_first>
    - frontend/src/app/workflows/page.tsx
    - frontend/src/components/ui/tabs.tsx
    - frontend/src/components/ui/table.tsx
    - frontend/src/components/workflows/WorkflowRunTimeline.tsx
  </read_first>
  <action>Add tabs for Overview, Runs, Schedules, and Templates. Build a compact runs table with status filters, workflow filter, timestamps, duration, cost, and links to run detail.</action>
  <acceptance_criteria>
    - Overview remains the default tab.
    - Runs table is scannable on desktop and usable on mobile.
    - Failed runs expose retry/open detail actions where supported.
    - No native browser dialogs or direct client `fetch` calls are introduced.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 4: Add schedule board UI</name>
  <files>frontend/src/components/workflows/WorkflowScheduleBoard.tsx, frontend/src/app/workflows/page.tsx</files>
  <read_first>
    - frontend/src/components/ui/status-badge.tsx
    - frontend/src/components/ui/button.tsx
    - frontend/src/components/ui/confirm-dialog.tsx
    - frontend/src/components/ui/table.tsx
  </read_first>
  <action>Build schedule board with upcoming grouped runs, trigger health, next run preview, enable/disable controls, run-now action, and links to latest run detail.</action>
  <acceptance_criteria>
    - Schedule health states are visible without opening the builder.
    - Enable/disable actions use existing confirm dialog patterns and audit reason payloads.
    - Schedule preview shows absolute timestamps and keeps text compact.
    - UI uses existing semantic colors and components.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 5: Complete Phase 4 verification and rebuild</name>
  <files>.planning/phases/04-scheduler-and-operations-console/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-scheduler-and-operations-console/04-CONTEXT.md
    - .planning/phases/04-scheduler-and-operations-console/04-01-PLAN.md
    - .planning/phases/04-scheduler-and-operations-console/04-02-PLAN.md
  </read_first>
  <action>Run full backend/frontend verification, rebuild containers, verify migrations/smoke checks, and record results.</action>
  <acceptance_criteria>
    - Backend and frontend verification commands pass.
    - Containers are rebuilt after implementation changes.
    - Live stack health and workflow scheduler smoke checks prove the latest version is running.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test pytest --no-cov -q tests/unit/services/test_workflow_schedule_board_api.py tests/unit/services/test_workflow_operations_api.py tests/unit/services/test_workflow_scheduler_api.py tests/unit/services/test_workflow_scheduler.py tests/unit/services/test_workflow_run_api.py tests/unit/services/test_workflow_api.py`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test black --check app/schemas/workflow.py app/services/workflows app/api/internal_v1/workflows.py tests/unit/services/test_workflow_schedule_board_api.py`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test isort --check-only app/schemas/workflow.py app/services/workflows app/api/internal_v1/workflows.py tests/unit/services/test_workflow_schedule_board_api.py`
- [ ] `cd frontend && npm run lint`
- [ ] `cd frontend && npm run check:colors`
- [ ] `cd frontend && npm run check:plumbing`
- [ ] `cd frontend && npm run build`
- [ ] `sudo docker compose up -d --build`
- [ ] `curl -fsS http://localhost:1080/health`
</verification>

<success_criteria>
- Users can operate scheduled workflows from `/workflows` without opening the builder.
- Schedule board shows upcoming runs, missed/failed schedule states, and next-run preview.
- Enable/disable, run-now, retry, and open-detail actions are available from operations surfaces.
</success_criteria>

## Artifacts This Plan Produces

- Schedule board API data.
- Runs tab/table.
- Schedule board UI.
- Phase 4 verification and rebuilt stack evidence.
