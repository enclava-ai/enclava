---
phase: 04-color-sweep
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/**
  - frontend/src/components/admin/**
  - frontend/src/app/audit/**
  - frontend/src/app/dashboard/api-keys/[id]/stats/page.tsx
autonomous: true
requirements: [COL-01, COL-02, COL-03]
user_setup: []
must_haves:
  truths:
    - "Admin, audit, pricing, user, connector, usage, and API-key stats surfaces have no legacy palette UI classes."
    - "Audit severities and admin statuses use semantic status treatment; categories stay neutral."
---

<objective>Sweep admin, audit, pricing, user, and API-key surfaces for semantic colors.</objective>
<context>
@.planning/phases/04-color-sweep/04-CONTEXT.md
@.planning/phases/04-color-sweep/04-RESEARCH.md
@.planning/phases/04-color-sweep/04-PATTERNS.md
@frontend/src/components/ui/status-badge.tsx
</context>
<tasks>
<task type="auto">
  <name>Task 1: Replace legacy colors in admin and audit pages</name>
  <files>frontend/src/app/admin/**, frontend/src/components/admin/**, frontend/src/app/audit/**</files>
  <action>Replace `empire-*`, raw status colors, raw neutral colors, and hardcoded spinner colors with semantic classes/components. Preserve audit action severity versus entity category distinctions.</action>
  <acceptance_criteria>
    - Scoped color grep over `frontend/src/app/admin frontend/src/components/admin frontend/src/app/audit` returns no planned color hits.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Sweep API-key usage stats</name>
  <files>frontend/src/app/dashboard/api-keys/[id]/stats/page.tsx</files>
  <action>Convert API-key stats cards, tabs, tables, bars, filters, loading states, and status labels to semantic classes and `StatusBadge` where appropriate.</action>
  <acceptance_criteria>
    - Scoped color grep over `frontend/src/app/dashboard/api-keys/[id]/stats/page.tsx` returns no planned color hits.
  </acceptance_criteria>
</task>
</tasks>
<verification>
- [ ] `cd frontend && npm run lint`
- [ ] scoped color greps for owned paths return zero planned hits
</verification>
