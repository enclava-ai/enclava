---
phase: 08-hardening-observability-and-release
plan: "03"
type: execute
wave: 3
depends_on:
  - "08-02"
files_modified:
  - docs/workflows/release.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/phases/08-hardening-observability-and-release/08-03-SUMMARY.md
  - .planning/phases/08-hardening-observability-and-release/08-VERIFICATION.md
autonomous: true
requirements: [WF-REL-01]
must_haves:
  truths:
    - "The workflow release has a concise operator-facing release checklist that points to the concrete verification commands."
    - "The final closeout reruns a broad workflow backend suite, frontend workflow/release guardrails, and rebuilt-container live smoke checks."
    - "Planning state marks Phase 8 complete only after verification passes."
    - "The milestone is left ready for audit/completion, with deferred limitations called out rather than hidden."
---

<objective>Close the Workflow Automation hardening phase by adding a release checklist, rerunning final verification, rebuilding containers, smoke-checking the live stack, and updating planning artifacts for milestone completion.</objective>

<threat_model>
<threat severity="high" id="T-08-03-01">The milestone could be marked complete without proving the rebuilt live stack is running the latest workflow UI/API.</threat>
<mitigation>Run `sudo docker compose up -d --build`, recreate nginx, and perform unauthenticated and authenticated smoke checks through `http://localhost:1080` before updating completion state.</mitigation>
<threat severity="medium" id="T-08-03-02">Release docs could scatter checks across operations and UAT docs, making future operators miss required steps.</threat>
<mitigation>Add `docs/workflows/release.md` as the release checklist that links operational concepts to exact commands and known limitations.</mitigation>
<threat severity="medium" id="T-08-03-03">A narrow final test run could miss regressions in earlier workflow phases.</threat>
<mitigation>Run all workflow-focused unit service tests plus the workflow UAT suite, then rerun frontend workflow guardrails and production build.</mitigation>
<threat severity="low" id="T-08-03-04">Planning artifacts could overstate release readiness by hiding deferred work.</threat>
<mitigation>Record the frontend browser/component test harness and distributed worker pool as deferred limitations while marking implemented v1.1 requirements complete.</mitigation>
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Add workflow release checklist doc</name>
  <files>docs/workflows/release.md</files>
  <read_first>
    - docs/workflows/operations.md
    - docs/workflows/uat.md
    - .planning/phases/08-hardening-observability-and-release/08-01-SUMMARY.md
    - .planning/phases/08-hardening-observability-and-release/08-02-SUMMARY.md
  </read_first>
  <action>Create a concise release checklist covering shipped workflow capabilities, automated checks, required rebuild/live smoke commands, authenticated API smoke expectations, operational maintenance checks, and known deferred items.</action>
  <acceptance_criteria>
    - The checklist includes the exact backend workflow test command and frontend guard/build commands.
    - The checklist includes the required Docker rebuild, nginx recreate, health, and `/workflows` smoke commands.
    - The checklist lists authenticated admin metrics, stale recovery, and retention dry-run API smokes.
    - Deferred limitations are explicit and do not contradict existing UAT docs.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Run final workflow release verification</name>
  <files>no production code changes</files>
  <action>Run the broad workflow backend unit service suite plus workflow UAT, then run frontend workflow guardrails and production build checks.</action>
  <acceptance_criteria>
    - Backend workflow service tests and workflow UAT pass in the Docker test container.
    - Backend formatting checks pass for the workflow release/UAT tests.
    - `npm run check:workflows`, `npm run lint`, `npm run check:colors`, `npm run check:plumbing`, and `npm run build` pass from `frontend/`.
    - `git diff --check -- . ':!CLAUDE.md' ':!design-proposal'` passes.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 3: Rebuild containers and run final live smoke</name>
  <files>runtime containers</files>
  <action>Rebuild the live Docker stack, recreate nginx, then run unauthenticated health/UI checks and authenticated workflow operations API smokes through nginx.</action>
  <acceptance_criteria>
    - `sudo docker compose up -d --build` succeeds.
    - `sudo docker compose up -d --force-recreate enclava-nginx` succeeds.
    - `curl -fsS http://localhost:1080/health` returns a healthy response.
    - `curl -fsSI http://localhost:1080/workflows` returns HTTP 200.
    - Authenticated workflow admin metrics, stale-lock recovery, and retention dry-run calls return structured success responses.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 4: Close Phase 8 planning state</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md, .planning/phases/08-hardening-observability-and-release/08-03-SUMMARY.md, .planning/phases/08-hardening-observability-and-release/08-VERIFICATION.md</files>
  <action>Write the 08-03 summary and final Phase 8 verification artifact, mark requirements and roadmap complete, update state to ready for milestone audit/completion, and record the release checklist decision.</action>
  <acceptance_criteria>
    - Phase 8 shows 3/3 plans complete and complete status.
    - State progress shows 8/8 phases, 22/22 plans, and 100 percent completion.
    - Requirements traceability is updated from planned to complete for v1.1.
    - The summary records exact final verification evidence and notes any residual warnings.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test sh -lc 'pytest --no-cov -q tests/unit/services/test_workflow_*.py tests/e2e/test_workflow_uat.py'`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test black --check tests/unit/services/test_workflow_release_criteria.py tests/e2e/test_workflow_uat.py`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test isort --check-only tests/unit/services/test_workflow_release_criteria.py tests/e2e/test_workflow_uat.py`
- [ ] `cd frontend && npm run check:workflows`
- [ ] `cd frontend && npm run lint`
- [ ] `cd frontend && npm run check:colors`
- [ ] `cd frontend && npm run check:plumbing`
- [ ] `cd frontend && npm run build`
- [ ] `git diff --check -- . ':!CLAUDE.md' ':!design-proposal'`
- [ ] `sudo docker compose up -d --build`
- [ ] `sudo docker compose up -d --force-recreate enclava-nginx`
- [ ] `curl -fsS http://localhost:1080/health`
- [ ] `curl -fsSI http://localhost:1080/workflows`
- [ ] Authenticated live smoke: workflow admin metrics, stale-lock recovery, and retention dry-run through nginx.
</verification>

<success_criteria>
- Operators have one release checklist for workflow verification and smoke checks.
- All workflow release checks pass after the final implementation state.
- The live rebuilt stack responds through nginx with the latest workflow UI/API.
- Phase 8 and v1.1 planning artifacts are ready for milestone audit/completion.
</success_criteria>
