---
phase: 02-design-system-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/globals.css
  - frontend/tailwind.config.js
autonomous: true
requirements: [DS-01, DS-02, DS-04]
user_setup: []
must_haves:
  truths:
    - "D-01/D-02: Slate Mono light and dark theme tokens are present while existing chart and font variables remain."
    - "D-03: Tailwind semantic colors use hsl(var(--x) / <alpha-value>) mappings so opacity modifiers work."
    - "D-04/D-05: Status roles expose solid and soft token pairs, and destructive aliases solid danger."
    - "D-10: Legacy empire/enclava palette definitions remain available until Phase 4 removes usage."
---

<objective>
Implement the Slate Mono semantic token foundation and Tailwind status mappings.
</objective>

<context>
@.planning/phases/02-design-system-foundation/02-CONTEXT.md
@.planning/phases/02-design-system-foundation/02-UI-SPEC.md
@.planning/phases/02-design-system-foundation/02-RESEARCH.md
@design-proposal/IMPLEMENTATION_PLAN.md
@frontend/src/app/globals.css
@frontend/tailwind.config.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add Slate Mono CSS variables and status pairs</name>
  <files>frontend/src/app/globals.css</files>
  <read_first>frontend/src/app/globals.css, .planning/phases/02-design-system-foundation/02-CONTEXT.md, design-proposal/IMPLEMENTATION_PLAN.md</read_first>
  <action>Update light and dark theme token blocks with Slate Mono semantic values. Add solid and soft status token pairs for success, warning, danger, and info. Make `--destructive` alias the danger solid pair. Preserve existing `--chart-*` and `--font-*` variables. Keep legacy utility classes for now unless they prevent compilation.</action>
  <acceptance_criteria>
    - `rg -n "--success-soft|--warning-soft|--danger-soft|--info-soft" frontend/src/app/globals.css` returns matches.
    - `rg -n "--chart-1|--font-sans|--destructive" frontend/src/app/globals.css` returns matches.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Add alpha-capable Tailwind semantic mappings</name>
  <files>frontend/tailwind.config.js</files>
  <read_first>frontend/tailwind.config.js, .planning/phases/02-design-system-foundation/02-PATTERNS.md</read_first>
  <action>Add a local `withA` helper and update semantic color mappings to use `hsl(var(--x) / <alpha-value>)`. Register border-strong, faint, accent-soft, success, warning, danger, and info colors. Preserve chart mappings and legacy `empire`/`enclava` definitions for Phase 4 compatibility.</action>
  <acceptance_criteria>
    - `rg -n "<alpha-value>|border-strong|accent-soft|success|warning|danger|info" frontend/tailwind.config.js` returns matches.
    - `rg -n "empire|enclava" frontend/tailwind.config.js` returns matches.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `rg -n "--success-soft|--warning-soft|--danger-soft|--info-soft" frontend/src/app/globals.css`
- [ ] `rg -n "<alpha-value>|success|warning|danger|info" frontend/tailwind.config.js`
- [ ] `cd frontend && npm run lint`
</verification>

<success_criteria>
- Semantic token foundation compiles.
- Status color vocabulary is available in Tailwind.
- Legacy palettes are retained for later cleanup.
</success_criteria>
