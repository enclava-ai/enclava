---
phase: 03-manual-execution-engine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/workflow.py
  - backend/app/models/__init__.py
  - backend/alembic/versions/035_add_workflow_run_runtime_fields.py
  - backend/app/schemas/workflow.py
  - backend/app/services/workflows/runtime.py
  - backend/app/services/workflows/__init__.py
  - backend/tests/unit/services/test_workflow_runtime.py
autonomous: true
requirements: [WF-RUN-01, WF-RUN-02, WF-RUN-04, WF-SEC-03]
must_haves:
  truths:
    - "Manual run creation persists queued workflow run records from published workflow versions."
    - "Runner claim semantics prevent two workers from claiming the same queued run."
    - "Run, step, event, and artifact state transitions are durable and restart-inspectable."
    - "Runtime redaction and budget metadata are represented before step handlers use them."
---

<objective>Implement run creation, runner claim loop, state transitions, and event/artifact persistence.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add runtime persistence fields</name>
  <files>backend/app/models/workflow.py, backend/alembic/versions/035_add_workflow_run_runtime_fields.py</files>
  <read_first>
    - backend/app/models/workflow.py
    - backend/alembic/versions/034_add_workflow_lifecycle.py
    - backend/app/schemas/workflow.py
  </read_first>
  <action>Add an additive migration and model fields for workflow run lock metadata, cancellation request metadata, retry ancestry, redaction policy, and budget/cost counters. Keep downgrade limited to the columns introduced by this migration.</action>
  <acceptance_criteria>
    - Migration revises `034_add_workflow_lifecycle`.
    - Runtime columns are nullable or have safe defaults for existing run rows.
    - Model imports and metadata creation still work with legacy workflow tables.
    - No scheduler-specific fields are introduced beyond generic lock/cancel/runtime metadata.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Add run schemas</name>
  <files>backend/app/schemas/workflow.py</files>
  <read_first>
    - backend/app/schemas/workflow.py
    - backend/app/models/workflow.py
  </read_first>
  <action>Add request/response schemas for manual run creation, run summaries, run detail, step run detail, artifact summary, event summary, retry/cancel actions, and redacted IO payloads.</action>
  <acceptance_criteria>
    - Schemas serialize enum-like status values consistently.
    - Run detail can represent queued, running, succeeded, failed, skipped, and cancelled runs.
    - Redacted input/output fields can distinguish hidden payloads from absent payloads.
    - Budget and duration fields are optional and stable for future UI.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 3: Implement runtime service core</name>
  <files>backend/app/services/workflows/runtime.py, backend/app/services/workflows/__init__.py</files>
  <read_first>
    - backend/app/services/workflows/service.py
    - backend/app/models/workflow.py
    - backend/app/db/database.py
    - backend/app/services/audit_service.py
  </read_first>
  <action>Create a workflow runtime service for manual run creation, run visibility checks, queued-run claiming, state transitions, event appends, step run creation, artifact creation, duration calculation, cancellation request marking, and redaction helpers. Do not execute real step handlers in this plan.</action>
  <acceptance_criteria>
    - Creating a manual run uses `WorkflowVersion.definition`, not mutable draft JSON.
    - Claiming a queued run marks it running, stores worker/lock metadata, and is safe under concurrent calls.
    - Completing/failing/cancelling a run persists run and event state.
    - Service raises typed errors for not found, permission denied, invalid lifecycle state, and conflict.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 4: Add runtime core tests</name>
  <files>backend/tests/unit/services/test_workflow_runtime.py</files>
  <read_first>
    - backend/tests/conftest.py
    - backend/tests/unit/services/test_workflow_lifecycle_service.py
    - backend/tests/unit/services/test_workflow_persistence.py
  </read_first>
  <action>Add async tests for manual run creation, published-version snapshot use, claim exclusivity, state transitions, event/artifact persistence, cancel request marking, and redaction behavior.</action>
  <acceptance_criteria>
    - Tests pass in the backend Docker test container.
    - Tests do not require live RAG, agent, notification, or LLM providers.
    - Tests prove persisted history remains inspectable after session refresh.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test pytest --no-cov -q tests/unit/services/test_workflow_runtime.py tests/unit/services/test_workflow_lifecycle_service.py tests/unit/services/test_workflow_persistence.py`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test alembic upgrade head`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test black --check app/models/workflow.py app/schemas/workflow.py app/services/workflows tests/unit/services/test_workflow_runtime.py alembic/versions/035_add_workflow_run_runtime_fields.py`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test isort --check-only app/models/workflow.py app/schemas/workflow.py app/services/workflows tests/unit/services/test_workflow_runtime.py alembic/versions/035_add_workflow_run_runtime_fields.py`
</verification>

<success_criteria>
- Manual run records can be created, claimed, transitioned, and inspected without executing step business logic yet.
- The runtime service is reusable by API, manual execution, and future scheduler/worker code.
- No frontend or scheduler behavior is introduced in this plan.
</success_criteria>

## Artifacts This Phase Produces

- Runtime migration and model fields.
- Workflow runtime service core.
- Run/detail schemas.
- Runtime core tests.
