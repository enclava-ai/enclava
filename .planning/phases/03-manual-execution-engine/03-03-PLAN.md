---
phase: 03-manual-execution-engine
plan: "03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - backend/app/api/internal_v1/workflows.py
  - backend/app/schemas/workflow.py
  - backend/app/services/workflows/runtime.py
  - backend/tests/unit/services/test_workflow_run_api.py
  - frontend/src/lib/api-client.ts
  - frontend/src/app/workflows/runs/[runId]/page.tsx
  - frontend/src/components/workflows/WorkflowRunTimeline.tsx
  - frontend/src/app/api/workflows/runs/[runId]/route.ts
autonomous: true
requirements: [WF-RUN-01, WF-RUN-04, WF-RUN-05, WF-SEC-03, WF-SEC-04]
must_haves:
  truths:
    - "Internal APIs expose manual run creation, run detail, retry, and cancellation."
    - "Run detail includes timeline, events, artifacts, redacted IO, duration, errors, and budget metadata."
    - "A compact frontend run timeline exists without adding Workflows to primary navigation yet."
    - "Lifecycle and run APIs preserve existing module smoke behavior."
---

<objective>Add run detail APIs, retry/cancel behavior, budget checks, redaction, and frontend run timeline.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add run API endpoints</name>
  <files>backend/app/api/internal_v1/workflows.py, backend/app/schemas/workflow.py</files>
  <read_first>
    - backend/app/api/internal_v1/workflows.py
    - backend/app/services/workflows/runtime.py
    - backend/tests/unit/services/test_workflow_api.py
  </read_first>
  <action>Add internal endpoints for manual run creation, optional immediate execution, run list by workflow, run detail, retry, cancel, and worker claim/execute helpers if needed for tests/future scheduler use.</action>
  <acceptance_criteria>
    - Routes live under `/api-internal/v1/workflows`.
    - Run creation returns the persisted run and optional executed detail when `execute_now` is true.
    - Errors distinguish not found, permission denied, validation/lifecycle errors, and conflict/cancel cases.
    - API operations write audit records for manual run request, retry, and cancel.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Add run detail serialization</name>
  <files>backend/app/services/workflows/runtime.py, backend/app/schemas/workflow.py</files>
  <read_first>
    - backend/app/models/workflow.py
    - backend/app/schemas/workflow.py
  </read_first>
  <action>Serialize run detail with ordered step runs, event timeline, artifact summaries, duration calculations, redacted input/output, retry ancestry, cancellation metadata, error details, and budget/cost metadata.</action>
  <acceptance_criteria>
    - Detail works for queued, running, succeeded, failed, skipped, and cancelled runs.
    - Strict redaction hides raw input/output payloads.
    - Default redaction masks obvious secret-like fields recursively.
    - Detail queries avoid async lazy-loading failures.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 3: Add retry and cancel service behavior</name>
  <files>backend/app/services/workflows/runtime.py</files>
  <read_first>
    - backend/app/services/workflows/runtime.py
    - backend/app/models/workflow.py
  </read_first>
  <action>Implement run retry and cancellation. Retry creates a new queued run from the same workflow version with `retry_of_run_id` set. Cancel marks queued runs cancelled immediately and running runs cancel-requested, then the runner stops before the next step.</action>
  <acceptance_criteria>
    - Only failed, cancelled, or succeeded-with-policy-allowed runs can be retried; invalid states fail clearly.
    - Queued cancellation is immediate and audited.
    - Running cancellation is visible in detail and honored at step boundaries.
    - Historical runs are not overwritten by retry.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 4: Add backend API tests</name>
  <files>backend/tests/unit/services/test_workflow_run_api.py</files>
  <read_first>
    - backend/tests/unit/services/test_workflow_api.py
    - backend/tests/unit/services/test_workflow_step_handlers.py
    - backend/tests/conftest.py
  </read_first>
  <action>Add API tests for manual run execution, detail serialization, retry, cancel, permission denial, redaction, artifacts, events, and audit rows.</action>
  <acceptance_criteria>
    - Tests pass in Docker with fake step dependencies.
    - Tests prove a non-owner without manage permission cannot run or inspect private workflow runs.
    - Tests prove run detail contains timeline and artifact data.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 5: Add compact frontend run timeline</name>
  <files>frontend/src/lib/api-client.ts, frontend/src/app/workflows/runs/[runId]/page.tsx, frontend/src/components/workflows/WorkflowRunTimeline.tsx, frontend/src/app/api/workflows/runs/[runId]/route.ts</files>
  <read_first>
    - frontend/src/lib/api-client.ts
    - frontend/src/components/ui/status-badge.tsx
    - frontend/src/components/ui/table.tsx
    - frontend/src/components/ui/card.tsx
    - frontend/src/components/ui/button.tsx
    - frontend/src/app/agents/page.tsx
    - frontend/src/lib/proxy-auth.ts
  </read_first>
  <action>Add a direct-linked workflow run detail page and timeline component that fetches run detail through a Next.js API proxy. Keep the UI compact: header, run status, duration/budget summary, step timeline, event list, artifact list, redacted IO panels, and retry/cancel buttons when allowed.</action>
  <acceptance_criteria>
    - No primary navigation entry is added in Phase 3.
    - UI uses existing components and restrained dashboard styling.
    - Text fits in compact panels on mobile and desktop.
    - Retry/cancel actions update the displayed detail without full page reload.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test pytest --no-cov -q tests/unit/services/test_workflow_run_api.py tests/unit/services/test_workflow_step_handlers.py tests/unit/services/test_workflow_runtime.py tests/unit/services/test_workflow_api.py tests/unit/services/test_workflow_contracts.py tests/test_modules.py::TestModuleIntegration::test_module_dependencies tests/test_modules.py::TestModuleAPI::test_modules_api_response_structure`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test black --check app/models/workflow.py app/schemas/workflow.py app/services/workflows app/api/internal_v1/workflows.py tests/unit/services/test_workflow_runtime.py tests/unit/services/test_workflow_step_handlers.py tests/unit/services/test_workflow_run_api.py`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test isort --check-only app/models/workflow.py app/schemas/workflow.py app/services/workflows app/api/internal_v1/workflows.py tests/unit/services/test_workflow_runtime.py tests/unit/services/test_workflow_step_handlers.py tests/unit/services/test_workflow_run_api.py`
- [ ] `cd frontend && npm run lint`
- [ ] `cd frontend && npm run check:colors`
- [ ] `cd frontend && npm run check:plumbing`
- [ ] `cd frontend && npm run build`
</verification>

<success_criteria>
- Users can manually run a published workflow and inspect the persisted result through internal APIs.
- Run detail exposes timeline, events, artifacts, redacted IO, duration, retry/cancel state, errors, and budget metadata.
- A compact frontend timeline exists without prematurely introducing the Phase 4 operations console.
</success_criteria>

## Artifacts This Phase Produces

- Manual run API endpoints.
- Run detail/retry/cancel tests.
- Direct-linked workflow run timeline UI.
