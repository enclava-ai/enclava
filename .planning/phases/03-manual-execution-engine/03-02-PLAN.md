---
phase: 03-manual-execution-engine
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/app/services/workflows/runtime.py
  - backend/app/services/workflows/steps.py
  - backend/app/services/workflows/__init__.py
  - backend/tests/unit/services/test_workflow_step_handlers.py
autonomous: true
requirements: [WF-RUN-02, WF-RUN-03, WF-RUN-04, WF-SEC-03, WF-SEC-04]
must_haves:
  truths:
    - "The runner executes published workflow steps in durable order."
    - "MVP handlers exist for RAG query, agent run, in-app notification, and no-results skip."
    - "Step input/output, artifacts, usage metadata, and errors are persisted with redaction."
    - "No real external provider calls are required for unit tests."
---

<objective>Implement MVP step handlers for RAG query, agent run, notification, and no-results skip.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add step handler contract</name>
  <files>backend/app/services/workflows/steps.py, backend/app/services/workflows/runtime.py</files>
  <read_first>
    - backend/app/services/workflows/registry.py
    - backend/app/schemas/workflow.py
    - backend/app/services/workflows/service.py
  </read_first>
  <action>Add a small handler protocol and structured step result object containing output data, artifacts, events, usage/cost metadata, and `skip_remaining`. Wire the runtime service to resolve handlers from step type and execute them sequentially.</action>
  <acceptance_criteria>
    - Unknown step types fail the step and run with actionable errors.
    - Step run rows capture pending, running, succeeded, failed, skipped, and cancelled statuses.
    - Step retries honor `WorkflowRetryPolicy.max_attempts` for transient handler failures.
    - Handler outputs are available to later steps by step key.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Implement `condition.no_results_skip`</name>
  <files>backend/app/services/workflows/steps.py</files>
  <read_first>
    - backend/app/services/workflows/templates.py
    - backend/app/schemas/workflow.py
  </read_first>
  <action>Implement the no-results condition step by reading the configured previous step output path and returning `skip_remaining` when the target value is absent or empty.</action>
  <acceptance_criteria>
    - Empty arrays, empty dicts, empty strings, and missing paths all trigger the skip.
    - Non-empty values allow execution to continue.
    - The step output records whether it matched and what action was taken.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 3: Implement `rag.query`</name>
  <files>backend/app/services/workflows/steps.py</files>
  <read_first>
    - backend/app/services/rag_service.py
    - backend/app/api/v1/rag.py
    - backend/app/utils/collection_access.py
  </read_first>
  <action>Implement RAG query handler using injected dependency first and existing RAG service patterns otherwise. Persist document count and a JSON artifact with redacted result metadata.</action>
  <acceptance_criteria>
    - Handler accepts `collection_id`, `query`, `limit`, `min_score`, and optional filters.
    - Handler output includes `documents` and `count`.
    - Collection access failures are recorded as failed step errors.
    - Unit tests can use a fake RAG dependency.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 4: Implement `agent.run`</name>
  <files>backend/app/services/workflows/steps.py</files>
  <read_first>
    - backend/app/modules/agent/main.py
    - backend/app/services/llm/service.py
    - backend/app/api/v1/llm.py
  </read_first>
  <action>Implement agent invocation with template rendering from prior step outputs. Use injected fake/adapter dependency in tests and existing agent module/service in production where available. Persist message output, usage metadata, cost metadata, and summary artifact.</action>
  <acceptance_criteria>
    - Prompt templates can reference previous step outputs using simple `{{step_key.path}}` placeholders.
    - Missing mapped values fail clearly instead of sending malformed prompts.
    - Usage/cost metadata is captured when returned by the agent/LLM path.
    - Budget guardrails can stop execution before the agent call when the workflow budget is already exceeded.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 5: Implement `notify.in_app`</name>
  <files>backend/app/services/workflows/steps.py</files>
  <read_first>
    - backend/app/services/notification_service.py
    - backend/app/models/notification.py
  </read_first>
  <action>Implement in-app notification handler using injected dependency first and notification persistence/service otherwise. Render title/body templates from prior step outputs and store notification IDs as output/artifact metadata.</action>
  <acceptance_criteria>
    - Recipient placeholders such as `{{owner_user_id}}` resolve from run/workflow context.
    - Notification delivery errors fail the step with a persisted message.
    - Tests can run without SMTP/webhook/slack configuration.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 6: Add step execution tests</name>
  <files>backend/tests/unit/services/test_workflow_step_handlers.py</files>
  <read_first>
    - backend/tests/unit/services/test_workflow_runtime.py
    - backend/tests/unit/services/test_workflow_contracts.py
  </read_first>
  <action>Add tests covering full successful execution, no-results skip, handler failure, retry attempts, artifact persistence, redaction behavior, budget halt, and persisted step ordering.</action>
  <acceptance_criteria>
    - Tests pass in Docker with injected fake dependencies.
    - Tests prove the Nightly RAG Summary shape can skip cleanly when RAG returns no documents.
    - Tests prove a successful RAG -> agent -> notification path persists outputs and artifacts.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test pytest --no-cov -q tests/unit/services/test_workflow_step_handlers.py tests/unit/services/test_workflow_runtime.py tests/unit/services/test_workflow_lifecycle_service.py`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test black --check app/services/workflows tests/unit/services/test_workflow_step_handlers.py`
- [ ] `sudo docker compose -f docker-compose.test.yml run --rm enclava-backend-test isort --check-only app/services/workflows tests/unit/services/test_workflow_step_handlers.py`
</verification>

<success_criteria>
- Published manual workflows can execute MVP step types end to end through service code.
- No-results workflows skip remaining work cleanly and still produce inspectable run history.
- Tests cover success, failure, retry, skip, redaction, artifact, and budget paths without live external services.
</success_criteria>

## Artifacts This Phase Produces

- Step handler module and handler result contract.
- Runtime step execution integration.
- MVP handler tests.
